# Job-Search Intelligence

A local-first web app that connects to your email (Gmail first), mines your entire job-search history, and answers questions about it, from "how many jobs did I apply to?" to "why am I getting rejected and what should I fix?", through a dashboard and a conversational RAG agent.

The supported end-to-end setup, sync, processing, dashboard, insights, grounded chat, and local wipe flow is documented in [`docs/operational-flow.md`](docs/operational-flow.md).
The current product workspace is `/`; older standalone routes are retained only as visibly marked diagnostic compatibility surfaces.

## Core principle

All factual job-search answers come from one clean `applications` table and its event timeline (`application_events`).
Dashboard numbers are deterministic SQL or typed Python logic.
The LLM synthesizes narrative insight only after deterministic facts are prepared, and it never produces authoritative counts or emits raw SQL for execution.

## Status

Phase 1 Gmail ingestion and Phase 2 classification plus aggregation slices have started on top of the Phase 0 scaffold.
The repository currently contains planning documents, root project metadata, the monorepo directory skeleton, the backend `uv` project scaffold, an initial FastAPI app factory (`backend/app/main.py`) with health, setup, provider config, local wipe-data, and Gmail auth-start routes, typed settings and API errors, the keyring-backed `SecretStore` adapter, provider registry, backend `LLMProvider` and `EmailProvider` Strategy interfaces, the backend `EmailSyncService` coordinator for paginated metadata sync and expired-cursor reconciliation, raw-email counting and idempotent upsert helpers on `EmailRepository`, a provider-neutral broad job-search candidate query DTO and static signal factory, a Gmail provider helper for building read-only OAuth authorization URLs, the exported `GmailEmailProvider` skeleton with readonly scope validation, public-safe not-implemented runtime errors, and a Gmail metadata lister for safe full-backfill and incremental history pages, retained email body HTML-to-text normalization, deterministic backfill reconciliation metrics and service-level sync-state status snapshots in `backend/app/services/sync_service.py`, an async SQLite engine module with sqlite-vec loading, `vec_version()` verification, and Phase 0 connection PRAGMAs, shared SQLite URL parsing, an Alembic migration environment, shared SQLite repository helpers, Phase 0 repository stubs with focused Pydantic domain DTO modules plus stable `app.models.records` aggregate exports, the `email_sync_state` migration, `SyncStateRepository`, `SyncService`, the synthetic fixture DTO contract, sample fixture, and SQLite fixture loader, the backend OpenAPI schema generator, root pre-commit configuration, backend and frontend CI workflows, and the frontend Vite React TypeScript shell with Orval API client generation, Recharts foundation, route-query helpers, shared accessible UI primitives, setup shell copy and overview sync action, empty `/dashboard` and `/chat` route shells plus the backend-backed `/insights` Q-41 recurring-feedback view, npm API contract, typecheck, lint, Vitest, build gate scripts, and a Chromium Playwright smoke harness for the current browser shell.
[JT-020 2026-07-05 v2] The repository currently contains planning documents, root project metadata, the monorepo directory skeleton, the backend `uv` project scaffold, an initial FastAPI app factory (`backend/app/main.py`) with health, setup, provider config, local wipe-data, and Gmail auth-start routes, typed settings and API errors, the keyring-backed `SecretStore` adapter, provider registry, backend `LLMProvider` and `EmailProvider` Strategy interfaces, the backend `EmailSyncService` coordinator for paginated metadata sync and expired-cursor reconciliation, a provider-neutral broad job-search candidate query DTO and static signal factory, a Gmail provider helper for building read-only OAuth authorization URLs, the exported `GmailEmailProvider` skeleton with readonly scope validation, public-safe not-implemented runtime errors, and a Gmail metadata lister for safe full-backfill and incremental history pages, retained email body HTML-to-text normalization, an async SQLite engine module with sqlite-vec loading, `vec_version()` verification, and Phase 0 connection PRAGMAs, shared SQLite URL parsing, an Alembic migration environment with sync sqlite-vec loading, shared SQLite repository helpers, Phase 0 repository stubs with focused Pydantic domain DTO modules plus stable `app.models.records` aggregate exports, the `email_sync_state` migration, the core schema migration for `raw_emails`, `email_classifications`, `applications`, `application_events`, `insights`, and `email_chunks`, `SyncStateRepository`, `SyncService`, the synthetic fixture DTO contract, sample fixture, and SQLite fixture loader, the backend OpenAPI schema generator, root pre-commit configuration, backend and frontend CI workflows, and the frontend Vite React TypeScript shell with Orval API client generation, Recharts foundation, route-query helpers, shared accessible UI primitives, setup shell copy and overview sync action, empty `/dashboard` and `/chat` route shells plus the backend-backed `/insights` Q-41 recurring-feedback view, npm API contract, typecheck, lint, Vitest, build gate scripts, and a Chromium Playwright smoke harness for the current browser shell.
[JT-020 2026-07-05 v1] The Alembic chain now includes `email_sync_state` followed by the core `raw_emails`, `email_classifications`, `applications`, `application_events`, `insights`, and `email_chunks` schema, and the migration environment loads sqlite-vec before applying vector-table revisions.
[JT-056 2026-07-05] Gmail OAuth callback handling now exchanges authorization codes, validates `gmail.readonly`, stores token material through the configured `SecretStore`, persists non-secret `email_connections` metadata, and returns only connection metadata.
The repository currently contains planning documents, the Phase 0 scaffold, Gmail read-only auth-start, safe metadata-only Gmail listing, retained-body fetching for caller-selected message refs, broad candidate selection before body retention, sync-service retained-body selection and chunking, raw-email repository helpers, sync-state persistence, deterministic reconciliation metrics, the core SQLite schema, synthetic fixture tooling, backend and frontend CI, and the current React shell.
[JT-020 2026-07-05 v2] The repository currently contains the core schema migration, sqlite-vec migration loading, `SyncStateRepository`, `SyncService`, `GmailEmailProvider` metadata and retained-body seams, the provider-neutral candidate query DTO, the backend OpenAPI generator, and the frontend Vite React TypeScript shell with Orval, Recharts, route-query helpers, shared UI primitives, setup copy, overview sync action, empty dashboard and chat route shells, npm gates, and Chromium Playwright smoke coverage.
[JT-020 2026-07-05 v1] The Alembic chain now includes `email_sync_state` followed by the core `raw_emails`, `email_classifications`, `applications`, `application_events`, `insights`, and `email_chunks` schema, and the migration environment loads sqlite-vec before applying vector-table revisions.
[JT-056 2026-07-05] Gmail OAuth callback handling now exchanges authorization codes, validates `gmail.readonly`, stores token material through the configured `SecretStore`, persists non-secret `email_connections` metadata, and returns only connection metadata.
[JT-062 2026-07-05] Gmail retained-body fetching now exists behind the provider seam for caller-selected refs; retained-body repository writes, token refresh, and default sync behavior now exist; product pages and remaining backend pieces fill in over subsequent Phase 1 tickets.
The repository currently contains planning documents, the Phase 0 scaffold, Gmail read-only auth-start, safe metadata-only Gmail listing, retained-body fetching for caller-selected message refs, broad candidate selection before body retention, sync-service retained-body selection and chunking, retained-body raw-email repository writes, sync-state persistence, deterministic reconciliation metrics, the core SQLite schema, synthetic fixture tooling, backend and frontend CI, and the current React shell.
[JT-020 2026-07-05 v2] The repository currently contains the core schema migration, sqlite-vec migration loading, `SyncStateRepository`, `SyncService`, `GmailEmailProvider` metadata and retained-body seams, the provider-neutral candidate query DTO, the backend OpenAPI generator, and the frontend Vite React TypeScript shell with Orval, Recharts, route-query helpers, shared UI primitives, setup copy, overview sync action, empty dashboard and chat route shells, npm gates, and Chromium Playwright smoke coverage.
[JT-020 2026-07-05 v1] The Alembic chain now includes `email_sync_state` followed by the core `raw_emails`, `email_classifications`, `applications`, `application_events`, `insights`, and `email_chunks` schema, and the migration environment loads sqlite-vec before applying vector-table revisions.
[JT-056 2026-07-05] Gmail OAuth callback handling now exchanges authorization codes, validates `gmail.readonly`, stores token material through the configured `SecretStore`, persists non-secret `email_connections` metadata, and returns only connection metadata.
[JT-062 2026-07-05] Gmail retained-body fetching now exists behind the provider seam for caller-selected refs; manual sync stores broad candidate and same-page candidate thread sibling retained bodies after metadata persistence; product pages and remaining backend pieces fill in over subsequent Phase 1 tickets.
Phase 0 (Groundwork) with early Phase 1 ingestion service slices.
The frontend shell keeps the root overview page intact, adds a backend-backed overview `Sync now` action with current sync status, adds a `/setup` first-run setup page with primary navigation, setup checklist, provider and classification mode copy, Gmail read-only boundary copy, privacy copy, a backend-backed Gmail OAuth start action, callback status from setup status, and explicit not-ready states until later setup persistence, metric, remaining insight, citation, and broader model-call behavior land.
[JT-020 2026-07-05 v2] Concrete Gmail provider behavior, dashboard, insights, chat, and remaining backend pieces fill in over subsequent Phase 0 and Phase 1 tickets.
[JT-073 2026-07-05 v1] Manual sync routes now orchestrate metadata sync through `EmailSyncService`, persist metadata-only raw-email rows without downgrading retained bodies, store broad candidate and same-page candidate thread sibling retained bodies when supported, store running and last-run status, and return typed not-configured responses when no Gmail connection is configured.
[JT-074 2026-07-05] Manual sync now resolves the latest non-reauth Gmail connection metadata from local SQLite, while product pages and remaining backend pieces fill in over subsequent Phase 1 tickets.
[JT-077 2026-07-05] The overview page now calls `GET /sync/status` and `POST /sync` through the generated frontend API client, shows returned sync counts and state, and displays typed backend errors without exposing Gmail data or tokens in the browser.
[JT-067 2026-07-05] Default sync now resolves the latest non-reauth Gmail connection, runs resumable full backfill until `email_backfill_state` is completed and an incremental cursor is promoted, and then uses incremental sync on later runs.
[JT-021 2026-07-05] The Alembic chain now creates `application_corrections` for audited manual aggregation overrides with constrained correction types, valid JSON snapshots, application delete cascade, and a per-application timestamp index.
[JT-022 2026-07-05] The Alembic chain now creates `chat_messages` for compact local chat history storage with constrained roles, JSON-array citation and tool-output payloads, and a conversation/time index for future history reads.
[JT-086 2026-07-05] The backend now includes an Azure OpenAI chat-completions adapter behind the `LLMProvider` seam, using non-secret Azure settings plus the configured `SecretStore` ref `azure_openai/api_key`.
[JT-087 2026-07-05] The backend now includes a concrete local `OllamaLLMProvider` chat adapter behind the `LLMProvider` seam; Azure OpenAI, Ollama embeddings, and downstream classification persistence remain later tickets.
[JT-099 2026-07-05] The Phase 2 golden-set fixture scaffold now lives at `backend/evals/golden_set.jsonl` with 30 private-data-free synthetic Gmail cases and a focused pytest contract check.
[JT-096 2026-07-05] The Alembic chain now creates `classification_runs`, and the backend exports `ClassificationRunRecord` plus `ClassificationRunRepository` for local per-run classification token and estimated-cost accounting.
[JT-084 2026-07-05] The sync runtime now persists broad job-search filter decisions in `email_filter_decisions` with public-safe reasons for auditability.
[JT-085 2026-07-06] The golden-set eval runner now also validates the metadata-only heuristic filter with `uv run python -m evals.run_eval --filter`, using each fixture row's `expected_to_pass_filter` label.
[JT-089 2026-07-05] Phase 2 classification now has a provider-neutral prompt contract in `backend/app/pipeline/classify.py`, strict `ClassificationPromptOutput` validation, and a synthetic golden-set eval scaffold under `backend/evals/`.
[JT-097 2026-07-05] `GET /classification/estimate` returns deterministic candidate counts, token estimates, and cost availability before a bulk classification pass without calling an LLM or exposing email content.
[JT-109 2026-07-05] `GET /classification/reprocessing-plan` returns deterministic model and prompt-version buckets for retained classification candidates, including whether the target model is configured before later runner work executes any LLM calls.
[JT-090 2026-07-05] Classification-specific DTOs now cover retained candidates, provider-neutral classification results, and stored classification records with category, confidence, model, prompt-version, unknown-field, repr-redaction, and timezone-aware timestamp validation.
[JT-095 2026-07-05] The backend now has a focused Phase 2 classification parser that turns clean structured LLM JSON into an `EmailClassificationRecord` plus typed extraction fields, and returns a public-safe `MalformedLLMExtraction` quarantine result for invalid JSON, invalid schema, duplicate keys, or incomplete generations.
[JT-095 2026-07-05] The synthetic golden-set eval runner lives at `backend/evals/run_eval.py`; later Phase 2 aggregation work now consumes accepted extraction facts.
[JT-091 2026-07-05] The backend now has `ClassificationService` for retained candidate emails; it builds the configured classification prompt, calls the configured `LLMProvider`, validates the full generation through the prompt-output contract, returns accepted extraction/classification DTOs or public-safe malformed metadata, and aggregates provider token usage without writing classification, application, or event rows.
[JT-094 2026-07-05] `StructuredExtractionService` now selects non-empty retained candidates, calls the configured `LLMProvider`, stores only accepted `email_classifications` rows, returns typed extraction facts and public-safe malformed results, and records one `classification_runs` accounting row per batch; later Phase 2 aggregation work now consumes those accepted extraction facts.
[JT-107 2026-07-06] Aggregation now derives each application `current_status` from the ordered application event timeline, combining persisted `application_events` with the current batch so incremental out-of-order evidence cannot regress status.
[JT-116 2026-07-06] `GET /applications/{id}` now returns one canonical `ApplicationRecord` from local SQLite through a thin FastAPI route, application detail service, repository lookup, typed 404 error, and generated OpenAPI client.
[JT-117 2026-07-06] `GET /applications/{id}/events` now returns the canonical ordered `ApplicationEventRecord` timeline for one application from local SQLite through a thin FastAPI route, application events service, repository lookups, typed 404 error, and generated OpenAPI client.
[JT-113 2026-07-06] Manual status and timeline-event correction endpoints now lock edited applications from automatic overwrite, create `application_corrections` audit rows, and protect edited events from conflicting aggregation reruns.
[JT-112 2026-07-06] `POST /applications/{application_id}/split` now moves selected events into a new manually locked application, locks the source application, recalculates source and target timeline dates, preserves an already locked source status, preserves source and target segmentation fields, records an audited split correction, and updates the generated OpenAPI client.
[JT-195 2026-07-07] Cached insight generation now includes the insight prompt version in `inputs_hash`, and the `story` insight answers Q-46 with chronological cited evidence from the recent 366-day search window anchored to the newest evidence.
[JT-186 2026-07-06] The backend now exposes `GET /insights` for latest cached insight records and `POST /insights/regenerate` for forced user-triggered regeneration through the configured LLM provider, deterministic insight inputs, citation-presence validation, repository cache writes, typed API errors, and generated OpenAPI client.
[2026-07-06] The frontend now includes a registry-backed `/features` developer inventory route that maps completed and in-progress frontend and backend surfaces, QA entry points, URL-backed filters, and topology summaries without adding product metrics or changing backend API behavior.
[JT-192 2026-07-07] Phase 4 insight storage now accepts the `strongest_weakest_signals` type for Q-43, and its input builder uses whole-history application and event evidence while preserving the shared cached-insight staleness behavior.
[JT-190 2026-07-07] The backend now exposes `GET /insights` and `POST /insights/regenerate`, adds `recurring_feedback` as the Q-41 cached insight type, scopes that insight to cited `feedback` timeline events, and the `/insights` frontend page can load and regenerate the Q-41 recurring-feedback insight.

## Architecture at a glance

| Area                  | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Backend               | FastAPI, Python 3.12, async                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Frontend              | React, TypeScript, Vite, shared accessible primitives                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Database              | SQLite (single local file) through async SQLAlchemy + aiosqlite                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Migrations            | Alembic with SQLite batch mode; sqlite-vec and virtual tables are hand-written revisions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Vector store          | sqlite-vec (embeddings in the same SQLite file)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| LLM providers         | Pluggable: Azure OpenAI and Ollama first; OpenAI and Anthropic later                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Provider registry     | Backend `app.providers.provider_registry` metadata for supported providers, non-secret requirements, and secret references                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| LLM provider seam     | Backend `app.providers.llm.LLMProvider` protocol with typed Pydantic generation DTOs; current concrete adapters cover Azure OpenAI chat completions and local-only Ollama chat with non-loopback base URLs rejected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Insights backend      | `GET /insights` lists latest cached narrative insights from local SQLite, including stale rows; `POST /insights/regenerate` forces one supported insight type through deterministic input building, the configured `LLMProvider`, citation-presence validation, and `InsightRepository` cache persistence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Classification prompt | `app.pipeline.classify` builds provider-neutral JSON-object `LLMGenerationRequest` values for retained email candidates, embeds `CLASSIFICATION_PROMPT_VERSION`, validates full provider generations before downstream storage or aggregation, and is orchestrated by `ClassificationService` without database writes                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Structured extraction | `backend/app/services/structured_extraction.py` runs one configured batch of non-empty retained candidates through `LLMProvider`, stores accepted `email_classifications` rows, returns typed extraction facts for later aggregation, quarantines malformed provider output, and writes `classification_runs` accounting                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Aggregation           | `app.pipeline.aggregate` builds frozen `ApplicationGroupingKey` DTOs from normalized company, normalized role, trimmed opaque provider thread IDs when present, and a 30-day UTC time-window fallback when the thread signal is missing; `AggregationService` upserts `applications` plus `application_events`, stores each event's extracted status, and derives `applications.current_status` from persisted plus current events in event-time order                                                                                                                                                                                                                                                                                                                                              |
| Insights              | `InsightGenerationService` builds deterministic fact and cited-evidence inputs for cached narrative insight synthesis; `GET /insights` lists the latest fresh cached insight per type, and `POST /insights/regenerate` can regenerate Q-41 `recurring_feedback` from `feedback` timeline events only                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Cached insights       | `backend/app/services/insights_service.py` builds deterministic fact and cited-evidence payloads, includes the insight prompt version in the cache `inputs_hash`, and scopes the Q-46 `story` insight to chronological evidence from the recent 366-day window anchored to the newest cited event or email timestamp                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Email providers       | `EmailProvider` protocol with typed auth, normalized metadata, cursor, candidate-query, and normalized retained-body DTOs; Gmail can build read-only OAuth authorization URLs, complete the callback with encrypted token persistence, refresh expired stored OAuth credentials through `SecretStore`, persist non-secret connection metadata, list safe metadata-only full-backfill and incremental history pages, fetch normalized retained bodies for selected refs when constructed with a `SecretStore`, resolve the default connected account for sync, run resumable full backfill until the replacement cursor is promoted, and store broad candidate plus same-page candidate thread sibling retained bodies during manual sync, while the browser triggers sync only through backend APIs |
| API style             | REST with an Orval-generated TypeScript client from OpenAPI, imported through `frontend/src/api`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Data contracts        | Pydantic v2 DTOs at every boundary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| API errors            | Typed `{"error": ...}` responses with sanitized validation, HTTP, internal, email-provider, and LLM-provider error details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Secret storage seam   | Backend `SecretStore` protocol with Pydantic `SecretRef` identifiers and `SecretStr` values                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Frontend charting     | Recharts through small accessible wrapper components; currently empty-state only until deterministic metrics APIs exist                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Frontend dashboard    | Empty `/dashboard` page shell with placeholder URL-backed filters and metric areas; no metrics API calls yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Feature inventory     | Registry-backed `/features` developer inventory for implemented frontend and backend surfaces, route-query filters, topology summaries, and QA guidance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Secret storage        | Backend `SecretStore` protocol with a default OS keyring adapter, Pydantic `SecretRef` identifiers, and `SecretStr` values                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Background sync       | APScheduler in-process                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| RAG agent             | LangGraph hybrid router (structured query + semantic retrieval)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Python tooling        | uv, ruff, mypy, pre-commit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

See `docs/groundwork-spec.md` for the full locked architecture and repository layout.

## Repository layout

```text
backend/          FastAPI app, pipeline, providers, security interfaces, repositories, evals, tests
backend/scripts/  backend operational scripts, including OpenAPI schema generation
frontend/         React + TypeScript + Vite app
docs/             source-of-truth product, architecture, setup, and convention documents
tickets/          issue manifest and templates
scripts/          repository-level developer and operational scripts
.github/          CI workflows
.pre-commit-config.yaml  root hooks for backend and frontend checks
```

## Privacy

- Local-first: app state lives in a single local SQLite file, and nothing leaves the machine except LLM API calls the user explicitly configures.
- Bring-your-own-credentials: no shared or bundled credentials, ever.
- Fernet fallback storage is documented in `docs/secret-storage.md`; set `JOBTRACKER_SECRET_STORE_BACKEND=fernet` when OS keyring is unavailable.
- Secrets are stored encrypted at rest through OS keyring by default and never logged; backend redaction helpers are exported from `app.security` for logging boundaries.
- If a provider reports an expired incremental cursor, the sync service restarts as full metadata reconciliation, persists page progress between pages, and exposes whether the run recovered from an expired cursor in sync status.
- Candidate selection uses provider-neutral static sender-domain, subject keyword, excluded-label, and same-page thread signals after metadata listing, so broad Gmail metadata queries do not expose snippets or body content.
- Candidate decisions carry deterministic scores and public-safe signal lists; excluded labels are hard rejections, and thread-promoted candidates expose only the static `thread_signal:candidate_thread` token instead of provider thread IDs.
- The same static keyword terms can also be checked against already-normalized retained body text through the candidate query when a later caller already has retained plain text.
- Filter decision audit rows store only static public-safe reasons such as `sender_domain:greenhouse.io`, `subject_keyword:interview`, `excluded_label:spam`, `thread_signal:candidate_thread`, and `no_filter_signal`; they do not store body text, snippets, raw subjects, or provider thread IDs.
- Retained email bodies are stored as normalized plain text: HTML MIME bodies are converted to text, raw HTML fields are rejected, and mislabelled plain-text bodies that still look like HTML fail validation.
- Email backfill stores broad metadata first; retained body text is fetched separately only for selected candidate, debugging, or reconciliation messages, and raw email DTOs track `metadata_only`, `retained`, or `debugging` retention state.
- Classification candidates carry only retained body text selected for the classification stage, keep that text out of repr output, and require timezone-aware `sent_at` values when present.
- Classification results and stored classification records reject unknown fields, strip model and prompt-version strings, require supported categories plus confidence from `0` through `1`, and require timezone-aware `classified_at` values.
- Structured extraction stores only accepted classification rows; malformed provider output returns public-safe quarantine metadata and does not write `email_classifications`, `applications`, or `application_events` rows.
- Raw email repository upserts are idempotent by provider message ID, and metadata-only replays preserve existing retained or debugging body text.
- Retained and debugging body upserts can create a minimal `raw_emails` row before metadata arrives, then later metadata-only replays fill the remaining metadata without downgrading the retained body.
- Classification prompts are built only for retained email candidates, request JSON-object output through `LLMProvider`, embed the prompt version, and instruct providers not to include raw SQL, counts, secrets, or provider payloads.
- Classification service calls return accepted classification and extraction DTOs or public-safe malformed metadata, aggregate provider token usage, and do not write `email_classifications`, `applications`, or `application_events` rows.
- Insight input payloads include deterministic facts and cited evidence only, suppress debugging retained body text, include the insight prompt version in the cache hash, and scope the Q-46 search story to recent chronological evidence instead of the whole historical inbox.
- Insight regeneration sends only deterministic facts and cited evidence from local SQLite through the configured `LLMProvider`, rejects incomplete, blank, or uncited provider output before cache writes, and maps provider failures to typed public-safe API errors.
- Application grouping keys use normalized company and role values, trim opaque provider thread IDs without case-folding them, prefer a present thread signal over date bucketing, and use a 30-day UTC time-window fallback only when the thread signal is missing.
- When extraction omits `event_at`, aggregation falls back to the raw email `sent_at` before the classification timestamp for both event upserts and no-thread grouping windows.
- Application `current_status` is derived from persisted plus current `application_events` ordered by event timestamp, raw email `sent_at`, and classification timestamp; event types map to statuses, while `feedback` preserves the current status unless extraction supplied one.
- Manual status and event edits set `applications.manual_lock`, record `status_edit` or `event_edit` audit snapshots in `application_corrections`, and return typed errors instead of overwriting missing applications, missing events, missing event source emails, no-op edits, or invalid event payloads.
- Event edits require at least one event field change, can explicitly clear `extract_note`, can clear `email_id` when changing to `ghost_inferred`, regenerate deterministic event IDs when `email_id`, `event_type`, or `event_at` changes, and recalculate the application status and last activity from the edited timeline.
- Aggregation reruns return manual conflicts instead of overwriting timeline rows protected by `event_edit` corrections when new email evidence disagrees with the edited event ID or its source email.
- Broad email metadata excludes body-derived snippets, and v1 ignores attachment content.
- Current Gmail full-backfill metadata listing requests Gmail `format=metadata` plus partial fields for message IDs, thread IDs, labels, size estimates, and selected headers only.
- Current Gmail incremental metadata listing uses Gmail `users.history.list` with `messageAdded` records from the stored history cursor, then fetches only the same metadata-only partial message fields.
- Gmail metadata-listing failures map to stable email-provider API error codes and `user_action` details so clients can guide reconnect, scope, retry, expired-cursor, invalid-response, and generic provider-failure recovery without exposing tokens or Gmail payloads.
- LLM provider failures map to stable public API error codes for unavailable providers, failed requests, invalid responses, and timeouts without exposing secrets or raw provider payloads.
- Gmail refreshes expired stored OAuth credentials before metadata listing or retained-body fetching, preserves the stored refresh token, validates the read-only scope, and returns only non-secret connection metadata.
- Gmail full-backfill pages capture a Gmail profile history cursor up front and return it only after the final page, so persisted incremental sync state does not skip remaining backfill pages.
- Gmail normalizes listed metadata before it crosses the provider boundary: opaque message IDs, thread IDs, and history cursors are trimmed without case changes; parsed addresses are lowercased and deduplicated; headers and display names are trimmed; Gmail system labels are canonicalized; custom label casing is preserved; and parsed timestamps become timezone-aware UTC.
  [JT-066 2026-07-05 v2] - Full-backfill state is stored in local SQLite as status, next page token, page and message counters, replacement cursor fields, timestamps, and public-safe failure text scoped by provider and account; completion records the final page and promotes its replacement cursor to sync state in one local transaction.
- Incremental sync anchors and in-progress page tokens are stored in local SQLite as opaque provider values scoped by provider and account; they are not OAuth token material or email content.
- `backend/.env.example` documents operational settings only; keep API keys, OAuth tokens, passwords, client secrets, and Google OAuth client JSON out of the repo.
- Local wipe-data path: `POST /local-data/wipe` clears configured local app data and derived artifacts after the request body confirms `{"confirmation":"wipe-local-data"}`.
- The wipe deletes `JOBTRACKER_DATA_DIR` recursively, and when `JOBTRACKER_DATABASE_URL` points to a local SQLite file outside that directory, it also deletes the database file plus `-wal`, `-shm`, and `-journal` sidecars.
- With the default Fernet paths, the wipe deletes encrypted secret payloads and the Fernet key inside `JOBTRACKER_DATA_DIR`; a custom `JOBTRACKER_FERNET_KEY_FILE` outside that directory is not a wipe target.
- For recursive wipe safety, a custom `JOBTRACKER_DATA_DIR` must either be named `.jobtracker` or contain a `.jobtracker-data` marker file before `POST /local-data/wipe` will delete it.
- Wipe safety checks preflight every target before deleting anything; unsafe directories, escaping symlinks, or unsafe external SQLite targets return a typed `400` error instead of a partial wipe.
- A successful wipe returns `{"status":"wiped","deleted_paths":[],"missing_paths":[]}`; invalid confirmation bodies return the standard typed `422` validation error.
- No telemetry.
- Gmail access is read-only (`gmail.readonly`) in v1.
- Google OAuth setup is documented in `docs/google-oauth-setup.md` and assumes a user-created Desktop app client with no shared or bundled credentials.

Historical retention note: before JT-065, README privacy guidance mentioned selected candidate or reconciliation messages but did not include debugging retained bodies.

## Repository guide

Read the source-of-truth documents in this order:

1. `docs/prd.md` - what is being built, for whom, and why (FR/NFR contract).
2. `docs/groundwork-spec.md` - the keystone architecture and phase roadmap.
3. `docs/questions.md` - the 54 questions the app must answer, tiered by capability.

Developer instructions:

- `AGENTS.md` - the canonical local agent guide with workflows and non-negotiable constraints.
- `docs/conventions.md` - baseline coding standards.
- `docs/google-oauth-setup.md` - Google OAuth setup guide for user-created Gmail credentials.
- `docs/llm-provider-setup.md` - Azure OpenAI and Ollama setup values, secret references, and provider boundaries.
- `docs/pipeline-diagnostic.md` - offline synthetic reproduction from synced emails through deterministic metrics.
- `docs/synthetic-fixtures.md` - private-data-free backend fixture format and SQLite loader for deterministic backend, aggregation, and dashboard smoke work.
- `docs/synthetic-fixtures.md` - private-data-free backend fixture formats, including SQLite smoke fixtures and the Phase 2 golden-set JSONL scaffold.
- `.editorconfig` - shared editor defaults.
- Project-local agent worktrees and scratch checkouts under `.worktrees/` are ignored; ticket source-of-truth files stay tracked under `tickets/`.

## Local Developer Quickstart

Use this path for a fresh local checkout in the current Phase 1 scaffold.
The backend SQLite engine loads sqlite-vec, applies Phase 0 connection setup, and the Alembic migration environment exists, but the first schema revision is later Phase 0 work.
Schema-specific upgrade behavior will apply once that revision lands.
[JT-020 2026-07-05 v3] The backend SQLite engine loads sqlite-vec, applies Phase 0 connection setup, and the Alembic migration chain includes sync-state and core schema revisions.
[JT-020 2026-07-05 v3] Schema-specific upgrade behavior applies through `uv run alembic upgrade head`.
Use this path for a fresh local checkout in Phase 0.
The backend SQLite engine loads sqlite-vec, applies Phase 0 connection setup, and the Alembic migration environment and first sync-state schema revision exist.
[JT-020 2026-07-05 v2] The backend SQLite engine loads sqlite-vec, applies Phase 0 connection setup, and the Alembic migration environment can load sqlite-vec for hand-written vector-table revisions.
[JT-020 2026-07-05 v1] The migration chain creates `email_sync_state` first, then the core `raw_emails`, `email_classifications`, `applications`, `application_events`, `insights`, and `email_chunks` schema.
[JT-021 2026-07-05] The migration chain also creates `application_corrections` for audited manual aggregation overrides.
[JT-022 2026-07-05] The migration chain also creates `chat_messages` for compact persisted chat history storage.
[JT-096 2026-07-05] The migration chain also creates `classification_runs` for completed-run classification token and estimated-cost accounting.
[JT-084 2026-07-05] The migration chain also creates `email_filter_decisions` for idempotent broad job-search filter audit rows.
[JT-073 2026-07-05 v1] The sync service uses the migrated `email_sync_state` and core `raw_emails` tables for manual metadata sync orchestration.
Run Alembic upgrades before using repository code that expects migrated tables.
The synthetic fixture loader can still populate caller-provided local SQLite connections for deterministic backend tests.

### 1. Clone the repository

```sh
git clone https://github.com/talibilat/job-search-intelligence.git
cd job-search-intelligence
```

Read the source-of-truth docs before implementing a ticket: `docs/prd.md`, `docs/groundwork-spec.md`, and `docs/questions.md`.

### 2. Install backend dependencies

The backend targets Python 3.12 and uses `uv` for dependency management.

```sh
cd backend
uv sync
```

Run backend commands from `backend/` with `uv run`.

### 3. Configure local backend environment

Copy the example only when you need local overrides.

```sh
cp .env.example .env
```

Do not put API keys, OAuth tokens, passwords, client secrets, or Google OAuth client JSON in `backend/.env`.
Keep Google OAuth client JSON outside the repository, for example at `~/.config/jobtracker/google-oauth-client.json`, and point `JOBTRACKER_GMAIL_CLIENT_CONFIG_FILE` at that path.
Keep `JOBTRACKER_GMAIL_SCOPES` set to `https://www.googleapis.com/auth/gmail.readonly` for v1 ingestion.
Default local LLM settings use `JOBTRACKER_LLM_PROVIDER=ollama`, `JOBTRACKER_CLASSIFICATION_MODE=local`, and `JOBTRACKER_OLLAMA_BASE_URL=http://127.0.0.1:11434`; the backend rejects non-local Ollama hosts and disables proxy routing for local Ollama calls.
`GET /auth/gmail` reads the client JSON to build a Google authorization URL, returns the generated state and requested read-only scope, and never returns the Google client secret or tokens.
`GET /auth/gmail/callback` consumes the issued state, exchanges the authorization code, stores access and refresh token material through the configured `SecretStore`, persists non-secret connection metadata, and returns no raw tokens.
Expired Gmail credentials are refreshed through the provider before metadata listing or retained-body fetching, with token material kept behind `SecretStore`.
The default `JOBTRACKER_DATA_DIR=./.jobtracker` is local app storage and is ignored by git.
Set `JOBTRACKER_SYNC_ON_OPEN=false` to disable the backend lifecycle scheduler; `JOBTRACKER_SYNC_INTERVAL_SECONDS` controls the APScheduler interval and must be at least 1 second.

### 4. Run the backend

From `backend/`:

```sh
uv run uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

Current backend endpoints include `GET /health`, `GET /setup/status`, `POST /setup`, `GET|PUT /config/providers`, `POST /config/providers/llm/health`, `GET /auth/gmail`, `GET /auth/gmail/callback`, `GET /classification/estimate`, `GET /classification/reprocessing-plan`, `POST /classification/run`, `GET /insights`, `POST /insights/regenerate`, `GET /applications/{id}`, `GET /applications/{id}/events`, `PATCH /applications/{application_id}/status`, `PATCH /applications/{application_id}/events/{event_id}`, `POST /applications/{application_id}/split`, `POST /applications/{application_id}/merge`, `POST /sync`, `GET /sync/status`, and `POST /local-data/wipe`.
The health endpoint returns `{"status":"ok"}`.
The setup status endpoint returns typed first-run readiness fields, derives `gmail_connected` from the latest non-reauth Gmail connection metadata when available, and never returns secrets.
The LLM provider health endpoint validates selected non-secret provider settings, calls the configured provider adapter with the selected chat and embedding model names, and returns a typed per-model availability response when an adapter is wired.
The insight generation service builds cached narrative insights through the configured chat model, and `weekly_actions` generations for Q-45 must return exactly three numbered next-week actions with allowed `citation_id` references before they are cached.
Insight cache input hashes include `INSIGHT_GENERATION_PROMPT_VERSION`, so prompt-template changes bypass legacy cached insight rows even when deterministic facts and evidence are unchanged.
The Gmail auth-start endpoint returns a provider-built Google authorization URL for `gmail.readonly` and maps missing, unreadable, or invalid client config files to typed `400` errors.
The Gmail callback endpoint validates state, exchanges the code through the Gmail provider, validates the returned `gmail.readonly` scope, stores token material through `SecretStore`, persists `email_connections` metadata, and maps provider failures to typed public-safe errors.
Gmail metadata listing and retained-body fetching automatically refresh expired stored credentials before calling Gmail, without returning raw token values.
The classification estimate endpoint counts retained candidate emails needing classification for the current model and prompt, estimates prompt and completion tokens with configured local heuristics, reports zero cost for local mode, and marks hosted-provider cost unavailable unless both configured pricing rates are non-zero.
The classification reprocessing plan endpoint partitions retained candidate emails into up-to-date, unclassified, stale-model, stale-prompt, and missing-target-model buckets for the configured target model and prompt version without calling an LLM or returning email content.
The classification run endpoint executes one structured extraction batch for retained candidates through the configured provider, stores only accepted `email_classifications`, records `classification_runs` accounting, and returns public-safe malformed-output counts without writing application or event rows.
The insights list endpoint returns the latest fresh cached narrative insights from local SQLite without calling an LLM.
The insights regenerate endpoint regenerates one requested insight type through the configured LLM provider, returns the saved insight plus evidence citation IDs, and for `recurring_feedback` saves an insufficient-evidence explanation instead of calling the LLM when fewer than two cited feedback events are available.
The manual application status edit endpoint updates `applications.current_status`, sets `manual_lock`, writes a `status_edit` correction snapshot, and returns typed 404 errors for missing applications.
The manual application event edit endpoint updates one timeline event, requires at least one event field change, validates any non-null `email_id` against `raw_emails`, can regenerate the deterministic event ID when identity fields change, sets `manual_lock`, writes an `event_edit` correction snapshot, recalculates `current_status` and `last_activity_at` from the timeline, and returns typed 400, 404, or 422 errors for invalid edits.
The application split endpoint moves selected event IDs from one incorrectly grouped source application into a new manually locked application, rejects missing or all-events selections with typed `409` errors, recalculates deterministic first/last activity dates for each resulting timeline, derives the new application's status from moved events, preserves an already manually locked source status, preserves corrected segmentation fields for deterministic dashboard breakdowns, and writes one `split` audit row in `application_corrections`.
The application merge endpoint moves one duplicate source application's events into a target application, recalculates the merged application summary, deletes the source application, and writes one `merge` audit row in `application_corrections`.
The manual sync endpoint resolves the latest non-reauth Gmail connection from SQLite, runs resumable full backfill until `email_backfill_state` is completed and an incremental cursor is promoted, persists public-safe filter decision audit rows, rejects concurrent syncs with typed `409`, maps provider failures to email-specific `401`, `403`, `409`, `429`, `502`, or `503` responses with a `user_action` detail, and returns `EmailSyncStatus` when runtime state can run.
The sync status endpoint returns the current or last-run `EmailSyncStatus` with state, provider, account, mode, start and finish timestamps, page count, message count, raw-email count, expired-cursor recovery, and public error text.
The wipe-data endpoint requires the exact confirmation phrase `wipe-local-data` before deleting configured local app data.
On backend startup, the FastAPI lifespan creates `SyncScheduler`; with sync-on-open enabled it schedules an immediate interval job, and with sync-on-open disabled it leaves the scheduler stopped.
The configured sync runtime wires Gmail provider access to SQLite repositories: it runs full backfill until a completed backfill has promoted an incremental cursor, persists public-safe filter decision audit rows, then runs incremental sync on later manual runs.

### 5. Install frontend dependencies

Use Node `^20.19.0 || ^22.13.0 || >=24`.

```sh
cd ../frontend
npm install
```

### 6. Run the frontend

From `frontend/`:

```sh
npm run dev
```

Vite serves the Phase 0 frontend shell locally, usually at `http://127.0.0.1:5173/`.
The overview page at `http://127.0.0.1:5173/` shows current sync status from `GET /sync/status` and lets you run manual Gmail sync through `POST /sync` when the backend and Gmail connection are configured.
The setup page is available at `http://127.0.0.1:5173/setup` and can start the read-only Gmail OAuth flow when the backend is running.
The empty dashboard page shell is available at `http://127.0.0.1:5173/dashboard`.
The insights page is available at `http://127.0.0.1:5173/insights`; it loads cached insights through `GET /insights` and can regenerate the Q-41 recurring-feedback insight through `POST /insights/regenerate` when the backend is running.
The feature status dashboard is available at `http://127.0.0.1:5173/features` and shows the registry-backed developer inventory with search, status, testable, scope, and tab filters encoded in the route query string.
The static chat page shell is available at `http://127.0.0.1:5173/chat`, with streaming chat, chat history API/UI behavior, retrieval, provider calls, and backend chat endpoints deferred to Phase 5.
Keep the backend running separately on `127.0.0.1:8000` when testing API-backed flows.

### 7. Smoke-check the local setup

With the backend running, verify the health route:

```sh
curl http://127.0.0.1:8000/health
```

Expected response:

```json
{ "status": "ok" }
```

Run the backend smoke test from `backend/`:

```sh
uv run pytest tests/test_health.py -q
```

Run the frontend toolchain gate from `frontend/` after backend dependencies have been synced:

```sh
npm run check
```

Install the Chromium browser for the frontend Playwright smoke suite once per machine:

```sh
npx playwright install chromium
```

Run the frontend Playwright smoke suite from `frontend/`:

```sh
npm run test:smoke
```

The smoke suite starts the Vite dev server on `127.0.0.1:4173` and asserts the currently rendered Phase 0 shell for setup copy, overview sync affordance, and dashboard empty-state coverage.

## Development Commands

The backend has an initial FastAPI app factory, typed API error DTOs in `backend/app/api/errors.py`, setup, auth-start, and provider config DTOs/routes/services, the `app.providers.provider_registry` metadata and validation seam, the `app.providers.llm.LLMProvider` strategy seam plus Azure OpenAI chat-completions adapter, typed settings in `backend/app/config.py`, the `SecretStore` protocol, keyring adapter, and redaction helpers in `backend/app/security/`, the `EmailProvider` contract, broad candidate query DTO, `GmailEmailProvider` skeleton, Gmail metadata lister, retained body HTML normalizer, and Gmail read-only OAuth authorization URL helper in `backend/app/providers/email/`, the `EmailSyncService` metadata-page coordinator in `backend/app/services/sync_service.py`, an async SQLite engine with sqlite-vec setup in `backend/app/db/engine.py`, shared SQLite URL parsing in `backend/app/db/sqlite_url.py`, an Alembic migration environment in `backend/app/db/migrations/`, shared SQLite repository helpers and repository stubs in `backend/app/db/repositories/`, `SyncStateRepository` for persisted provider cursors, focused domain DTO modules under `backend/app/models/` with stable aggregate exports in `backend/app/models/records.py`, `SyncService` for sync cursor business flow, synthetic fixture DTOs in `backend/app/models/synthetic_fixture.py`, the `SyntheticFixtureRepository` loader in `backend/app/db/repositories/synthetic_fixture.py`, a sample fixture in `backend/tests/fixtures/synthetic/basic_job_search.json`, `backend/scripts/generate_openapi.py` for deterministic OpenAPI schema generation, a `backend/pyproject.toml` with strict mypy defaults plus `uv` project metadata, `backend/pytest.ini`, and `backend/.env.example` documenting expected v1 operational settings.
[JT-020 2026-07-05 v2] The backend has an initial FastAPI app factory, typed API error DTOs in `backend/app/api/errors.py`, setup, auth-start, and provider config DTOs/routes/services, the `app.providers.provider_registry` metadata and validation seam, the `app.providers.llm.LLMProvider` strategy seam, typed settings in `backend/app/config.py`, the `SecretStore` protocol, keyring adapter, and redaction helpers in `backend/app/security/`, the `EmailProvider` contract, broad candidate query DTO, `GmailEmailProvider` skeleton, Gmail metadata lister, retained body HTML normalizer, and Gmail read-only OAuth authorization URL helper in `backend/app/providers/email/`, the `EmailSyncService` metadata-page coordinator in `backend/app/services/sync_service.py`, an async SQLite engine with sqlite-vec setup in `backend/app/db/engine.py`, shared SQLite URL parsing in `backend/app/db/sqlite_url.py`, an Alembic migration environment in `backend/app/db/migrations/` with sync sqlite-vec loading for vector-table revisions, shared SQLite repository helpers and repository stubs in `backend/app/db/repositories/`, `SyncStateRepository` for persisted provider cursors, focused domain DTO modules under `backend/app/models/` with stable aggregate exports in `backend/app/models/records.py`, `SyncService` for sync cursor business flow, synthetic fixture DTOs in `backend/app/models/synthetic_fixture.py`, the `SyntheticFixtureRepository` loader in `backend/app/db/repositories/synthetic_fixture.py`, a sample fixture in `backend/tests/fixtures/synthetic/basic_job_search.json`, `backend/scripts/generate_openapi.py` for deterministic OpenAPI schema generation, a `backend/pyproject.toml` with strict mypy defaults plus `uv` project metadata, `backend/pytest.ini`, and `backend/.env.example` documenting expected v1 operational settings.
The backend has an initial FastAPI app factory, typed API error DTOs in `backend/app/api/errors.py`, setup, auth-start, and provider config DTOs/routes/services, the `app.providers.provider_registry` metadata and validation seam, the `app.providers.llm.LLMProvider` strategy seam, typed settings in `backend/app/config.py`, the `SecretStore` protocol, keyring adapter, and redaction helpers in `backend/app/security/`, the `EmailProvider` contract, `GmailEmailProvider` skeleton, retained body HTML normalizer, and Gmail read-only OAuth authorization URL helper in `backend/app/providers/email/`, the `EmailSyncService` metadata-page coordinator and deterministic backfill reconciliation metrics in `backend/app/services/sync_service.py`, provider-neutral candidate query DTOs, an async SQLite engine in `backend/app/db/engine.py`, shared SQLite URL parsing in `backend/app/db/sqlite_url.py`, an Alembic migration environment in `backend/app/db/migrations/`, shared SQLite repository helpers and repository stubs in `backend/app/db/repositories/`, raw-email counting helpers on `EmailRepository`, focused domain DTO modules under `backend/app/models/` with stable aggregate exports in `backend/app/models/records.py`, synthetic fixture DTOs in `backend/app/models/synthetic_fixture.py`, the `SyntheticFixtureRepository` loader in `backend/app/db/repositories/synthetic_fixture.py`, a sample fixture in `backend/tests/fixtures/synthetic/basic_job_search.json`, `backend/scripts/generate_openapi.py` for deterministic OpenAPI schema generation, a `backend/pyproject.toml` with strict mypy defaults plus `uv` project metadata, `backend/pytest.ini`, and `backend/.env.example` documenting expected v1 operational settings.
The database engine creates the configured local database parent directory, accepts `sqlite:///` or `sqlite+aiosqlite:///` file-backed URLs, registers `foreign_keys=ON`, `journal_mode=WAL`, `synchronous=NORMAL`, and a 5000 ms busy timeout, and exposes a transaction context manager for future repositories and services.
The backend has an initial FastAPI app factory, typed API error DTOs in `backend/app/api/errors.py`, setup and provider config DTOs/routes/services, the `app.providers.provider_registry` metadata and validation seam, the `app.providers.llm.LLMProvider` strategy seam, typed settings in `backend/app/config.py`, the `SecretStore` protocol, keyring adapter, and redaction helpers in `backend/app/security/`, the `EmailProvider` contract in `backend/app/providers/email/`, an async SQLite engine with sqlite-vec setup in `backend/app/db/engine.py`, shared SQLite URL parsing in `backend/app/db/sqlite_url.py`, an Alembic migration environment in `backend/app/db/migrations/`, shared SQLite repository helpers and repository stubs in `backend/app/db/repositories/`, focused domain DTO modules under `backend/app/models/` with stable aggregate exports in `backend/app/models/records.py`, synthetic fixture DTOs in `backend/app/models/synthetic_fixture.py`, the `SyntheticFixtureRepository` loader in `backend/app/db/repositories/synthetic_fixture.py`, a sample fixture in `backend/tests/fixtures/synthetic/basic_job_search.json`, `backend/scripts/generate_openapi.py` for deterministic OpenAPI schema generation, a `backend/pyproject.toml` with strict mypy defaults plus `uv` project metadata, `backend/pytest.ini`, and `backend/.env.example` documenting expected v1 operational settings.
The backend has an initial FastAPI app factory, typed API error DTOs in `backend/app/api/errors.py`, setup and provider config DTOs/routes/services, the `app.providers.provider_registry` metadata and validation seam, the `app.providers.llm.LLMProvider` strategy seam, typed settings in `backend/app/config.py`, the `SecretStore` protocol, keyring adapter, and redaction helpers in `backend/app/security/`, the `EmailProvider` contract, `GmailEmailProvider` skeleton, and retained body HTML normalizer in `backend/app/providers/email/`, the `EmailSyncService` metadata-page coordinator in `backend/app/services/sync_service.py`, an async SQLite engine with sqlite-vec setup in `backend/app/db/engine.py`, shared SQLite URL parsing in `backend/app/db/sqlite_url.py`, an Alembic migration environment in `backend/app/db/migrations/`, shared SQLite repository helpers and repository stubs in `backend/app/db/repositories/`, `EmailRepository` raw-email upserts, `SyncStateRepository` for persisted provider cursors, focused domain DTO modules under `backend/app/models/` with stable aggregate exports in `backend/app/models/records.py`, `SyncService` for sync cursor business flow and status snapshots, synthetic fixture DTOs in `backend/app/models/synthetic_fixture.py`, the `SyntheticFixtureRepository` loader in `backend/app/db/repositories/synthetic_fixture.py`, a sample fixture in `backend/tests/fixtures/synthetic/basic_job_search.json`, `backend/scripts/generate_openapi.py` for deterministic OpenAPI schema generation, a `backend/pyproject.toml` with strict mypy defaults plus `uv` project metadata, `backend/pytest.ini`, and `backend/.env.example` documenting expected v1 operational settings.
The database engine creates the configured local database parent directory, accepts `sqlite:///` or `sqlite+aiosqlite:///` file-backed URLs, loads sqlite-vec from the bundled runtime dependency or `JOBTRACKER_SQLITE_VEC_EXTENSION_PATH`, verifies availability with `vec_version()`, registers `foreign_keys=ON`, `journal_mode=WAL`, `synchronous=NORMAL`, and a 5000 ms busy timeout, and exposes a transaction context manager for future repositories and services.
The backend database schema is migration-owned; `uv run alembic ensure_version` can initialize the Alembic version table without applying schema revisions.
The first Alembic schema revision creates `email_sync_state`; run `uv run alembic upgrade head` before using sync-state repository behavior against a fresh local database.
[JT-066 2026-07-05 v2] The next Phase 1 schema revision creates `email_backfill_state`; run `uv run alembic upgrade head` before using backfill-state repository behavior against a fresh local database.
[JT-020 2026-07-05 v2] The first Alembic schema revision creates `email_sync_state`; the next core schema revision creates `raw_emails`, `email_classifications`, `applications`, `application_events`, `insights`, and `email_chunks` with SQLite constraints, foreign keys, lookup indexes, and a 1536-dimensional sqlite-vec embedding column.
[JT-056 2026-07-05] The Alembic chain also creates `email_connections` for non-secret provider account metadata and credential `SecretRef` fields.
[JT-021 2026-07-05] The Alembic chain also creates `application_corrections` with a cascade foreign key to `applications`, constrained correction types, valid JSON before/after snapshots, and an `(application_id, created_at)` lookup index.
[JT-022 2026-07-05] The Alembic chain also creates `chat_messages` with constrained `user`, `assistant`, `tool`, and `system` roles, valid JSON-array citation and tool-output payloads, and a `(conversation_id, created_at)` lookup index.
[JT-096 2026-07-05] The Alembic chain also creates `classification_runs` with constrained counts, token totals, estimated USD cost, and lookup indexes on start time and provider/model.
[JT-084 2026-07-05] The Alembic chain also creates `email_filter_decisions` with constrained `broad_job_search` strategy and `candidate | rejected` outcomes, `raw_emails` cascade deletion, and outcome/strategy indexes.
[JT-107 2026-07-06] The Alembic chain also adds nullable `application_events.extracted_status` with the same status enum as `applications.current_status` so aggregation can replay status-bearing event history deterministically.
[JT-192 2026-07-07] The Alembic chain also extends the `insights.type` constraint to allow `strongest_weakest_signals` cache rows and keeps the existing application, event, and cited raw-email staleness triggers intact.
[JT-190 2026-07-07] The Alembic chain also adds `recurring_feedback` to constrained `insights.type` values and recreates the insight staleness triggers around the constraint migration.
[JT-020 2026-07-05 v2] Run `uv run alembic upgrade head` before using repository behavior against a fresh local database.
[JT-073 2026-07-05 v1] The backend sync route and service now cover manual metadata sync orchestration, metadata-only upserts, broad candidate retained-body upserts, persisted provider cursor and page progress, running and failed status, and typed not-configured and concurrency API responses.
[JT-067 2026-07-05] The configured sync runtime now resolves persisted Gmail connection metadata, records full-backfill page progress only after metadata and retained-body writes complete, promotes the final replacement cursor, and fails safely when retained-body fetching fails.

- Backend: `uv sync` then `uv run <command>` from `backend/`. The project targets Python 3.12, declares `fastapi`/`uvicorn`, `apscheduler`, `keyring`, `cryptography`, `sqlalchemy[asyncio]`, `aiosqlite`, `sqlite-vec`, and `alembic` as runtime dependencies, and uses `ruff`, `mypy`, `pytest`, and `pre-commit` as dev-dependency verification tooling; `backend/pyproject.toml` also holds the strict mypy defaults.
- Backend tests: `uv run pytest` from `backend/`; `backend/pytest.ini` discovers `tests/` and sets `pythonpath = .` so tests import the local `app` package deterministically.
- Repository base contract: import `BaseRepository` and the shared `SqlParameters` type from `app.db.repositories`; `uv run pytest tests/test_repository_base.py -v` verifies typed row mapping, parameterized statements, transactions, and the package export contract.
- SQLite engine test: `uv run pytest tests/test_sqlite_engine.py -v` from `backend/` verifies async engine creation, sync-to-async SQLite URL normalization, sqlite-vec loading and `vec_version()` availability, connection PRAGMAs, transaction commit/rollback behavior, and local database parent directory creation.
- Alembic migration test: `uv run pytest tests/test_alembic_migrations.py -v` from `backend/` verifies the Alembic config, SQLite batch mode, sync migration URL normalization, virtual-table autogenerate exclusion, and SQLite version-table creation.
- [JT-096 2026-07-05] Alembic migration test: `uv run pytest tests/test_alembic_migrations.py -v` from `backend/` verifies the Alembic config, SQLite batch mode, sync migration URL normalization, virtual-table autogenerate exclusion, SQLite version-table creation, core schema creation, email connection schema creation, classification run accounting schema creation and constraints, sqlite-vec migration loading, nullable ghost-inferred event email references, and downgrade behavior.
- [JT-021 2026-07-05] Alembic migration test: `uv run pytest tests/test_alembic_migrations.py -v` from `backend/` verifies `application_corrections` table creation, foreign key shape, correction type validation, JSON snapshot validation, cascade behavior, and downgrade cleanup.
- [JT-022 2026-07-05] Alembic migration test: `uv run pytest tests/test_alembic_migrations.py -v` from `backend/` verifies `chat_messages` table creation, chat role validation, JSON-array citation and tool-output validation, the conversation/time index, and downgrade cleanup.
- [JT-084 2026-07-05] Alembic migration test: `uv run pytest tests/test_alembic_migrations.py -v` from `backend/` verifies `email_filter_decisions` table creation, foreign key shape, constrained strategy and outcome values, and lookup indexes.
- Synthetic fixture format test: `uv run pytest tests/test_synthetic_fixture_format.py -v` from `backend/` verifies the versioned private-data-free fixture contract, duplicate ID rejection, cross-reference validation, unknown-field rejection, shared raw-email retention enum validation, retained-body repr redaction, and the checked-in sample fixture.
- [JT-020 2026-07-05 v3] Synthetic fixture format test: `uv run pytest tests/test_synthetic_fixture_format.py -v` from `backend/` also verifies that only `ghost_inferred` events may use `email_id: null`; evidence-backed events require source email references.
- Synthetic fixture loader test: `uv run pytest tests/test_synthetic_fixture_loader.py -v` from `backend/` verifies JSON fixture loading into the four core SQLite tables, typed per-table load counts, repository reads, and idempotent reloads.
- [JT-020 2026-07-05 v3] Synthetic fixture loader test: `uv run pytest tests/test_synthetic_fixture_loader.py -v` from `backend/` also verifies loading a `ghost_inferred` application event without an email reference.
- Golden-set fixture test: `uv run pytest tests/test_golden_set_fixture.py -v` from `backend/` verifies the private-data-free JSONL eval scaffold, synthetic sender domains, unique case IDs, negative examples, and core positive category coverage.
- Backfill reconciliation service: `build_backfill_reconciliation_metrics` compares provider metadata pages with local `raw_emails` rows for one provider, reports provider page count, total and unique provider message counts, duplicate provider messages, local raw-email count, local-vs-provider delta, missing local messages, extra local messages, and a deterministic `reconciled` flag; `uv run pytest tests/test_sync_service_reconciliation.py -v` verifies duplicate provider paging and missing or extra local rows.
- Repository stubs: import `EmailRepository`, `EmailConnectionRepository`, `ApplicationRepository`, `EventRepository`, `InsightRepository`, `CorrectionRepository`, `ChatRepository`, and `ClassificationRunRepository` from `app.db.repositories`; `uv run pytest tests/test_repository_stubs.py -v` verifies package exports, raw-email retention-state invariants, retained-body repr redaction, email connection upserts, and row-to-record mapping for focused domain DTOs.
- Classification run repository test: `uv run pytest tests/test_classification_run_repository.py -v` from `backend/` verifies completed-run accounting upsert, fetch, replacement by run ID, datetime serialization, and estimated-cost decimal preservation.
- Domain DTOs: `uv run pytest tests/test_domain_dtos.py -v` from `backend/` verifies package exports, classification category and confidence validation, application salary-range validation, email chunk embedding shape and repr redaction, JSON object parsing for correction snapshots, and chat message role plus JSON-array validation.
- Filter decision repository test: `uv run pytest tests/test_filter_decision_repository.py -v` from `backend/` verifies idempotent upsert of filter outcome and reason, DTO strategy validation, and database strategy constraints.
- Classification prompt contract test: `uv run pytest tests/test_classification_prompt_contract.py -v` from `backend/` verifies provider-neutral JSON-object request construction, prompt-version embedding, retained-email prompt payload formatting, and strict `ClassificationPromptOutput` validation for extraction fields and malformed provider output.
- Classification service test: `uv run pytest tests/test_classification_service.py -q` from `backend/` verifies retained candidate prompt construction, configured model and prompt-version use, accepted result extraction/classification DTOs, public-safe malformed output, and provider token aggregation.
- Classification DTOs: `uv run pytest tests/test_classification_dtos.py -v` from `backend/` verifies candidate/result/record exports, retained-body repr redaction, unknown-field rejection, category and confidence validation, model and prompt-version stripping, and timezone-aware classification timestamps.
- Sync-state persistence test: `uv run pytest tests/test_sync_state.py -v` from `backend/` verifies Alembic creates `email_sync_state`, cursors upsert per provider account, and repository reads require migrated schema.
  [JT-066 2026-07-05 v2] - Backfill-state persistence test: `uv run pytest tests/test_backfill_state.py -v` from `backend/` verifies Alembic creates `email_backfill_state`, backfill state upserts per provider account, completed pages require replacement cursors, failures store public-safe errors while preserving progress, and final-page cursor promotion shares one SQLite transaction with page recording.
- Ingestion smoke test: `uv run pytest tests/test_ingestion_smoke.py -v` from `backend/` verifies provider metadata pagination, idempotent raw-email upserts, metadata replay preserving retained body text, and service-level sync-state status snapshots.
- Sync API test: `uv run pytest tests/test_sync_api.py -v` from `backend/` verifies manual sync and status routes, typed not-configured, concurrency, and email-provider errors, persisted Gmail connection lookup, dependency-wired repositories, metadata persistence, full-backfill runtime selection, and status-store behavior.
- Classification estimate API test: `uv run pytest tests/test_classification_estimate_api.py -v` from `backend/` verifies candidate filtering, stale model and prompt inclusion, local zero-cost behavior, unavailable hosted pricing, and missing-database read behavior.
- Classification reprocessing API test: `uv run pytest tests/test_classification_reprocessing_api.py -v` from `backend/` verifies deterministic version bucket partitioning, unchanged current rows, missing target-model blocking, raw-only database handling, and missing-database read behavior.
- Application detail API test: `uv run pytest tests/test_application_detail_api.py -v` from `backend/` verifies `GET /applications/{id}` returns an `ApplicationRecord`, `GET /applications/{id}/events` returns ordered `ApplicationEventRecord` timelines and empty timelines, missing applications return the standard typed 404, OpenAPI documents the response schemas, and repository lookups do not hide a missing `applications` table.
- Manual application edit API test: `uv run pytest tests/test_manual_application_edit_api.py -v` from `backend/` verifies status edits, event edits, audit responses, manual locks, typed not-found, bad-request, validation errors, and OpenAPI response documentation for `PATCH /applications/{application_id}/status` and `PATCH /applications/{application_id}/events/{event_id}`.
- Manual application edit service test: `uv run pytest tests/test_manual_application_edit_service.py -v` from `backend/` verifies status/event correction audit snapshots, locked applications, missing source-email rejection, no-op rejection, event ID regeneration, status replay, stale extracted-status clearing, and manual-conflict protection during aggregation reruns.
- Application split API test: `uv run pytest tests/test_application_split_api.py -v` from `backend/` verifies `POST /applications/{application_id}/split` moves selected events into a new application, records the split audit correction, preserves source and target segmentation fields, derives deterministic timeline summaries, preserves locked source status, rejects invalid repository wiring, and documents the split schemas in OpenAPI.
- Structured extraction service test: `uv run pytest tests/test_structured_extraction_service.py -v` from `backend/` verifies retained candidate selection, configured batch size and prompt version usage, accepted classification storage, malformed-output quarantine behavior, token/cost run accounting, and local zero-cost accounting.
- Insights API test: `uv run pytest tests/test_insights_api.py -v` from `backend/` verifies `GET /insights` lists fresh cached insights without LLM calls and `POST /insights/regenerate` returns regenerated Q-41 recurring-feedback content plus citation IDs through the service dependency seam.
- Metrics breakdown API test: `uv run --project backend pytest backend/tests/test_metrics_breakdown_api.py -q` verifies `GET /metrics/breakdown` returns deterministic dimension rows and documents its response schemas in OpenAPI.
- Insight input and generation service tests: `uv run pytest tests/test_insight_input_builder.py tests/test_insight_generation_service.py -q` from `backend/` verify deterministic insight facts, cited evidence, debugging-body suppression, prompt-version cache invalidation, Q-46 story evidence scoping, and grounded prompt construction.
- Aggregation service test: `uv run --project backend pytest backend/tests/test_aggregation_service.py -q` verifies idempotent application and event upserts, event-time status derivation across persisted plus current events, manual-lock conflict preservation, and missing-`event_at` timestamp fallback behavior.
- Backend smoke test: `uv run pytest tests/test_health.py -q` from `backend/`.
- Email provider contract test: `uv run pytest tests/test_email_provider_contract.py -v` from `backend/` verifies the provider boundary keeps OAuth token material behind `SecretRef`, separates metadata from retained body fetching, supports full and incremental cursor shapes, excludes body-derived metadata snippets, excludes attachment content, normalizes HTML bodies to retained plain text, and rejects raw HTML retention fields.
- Sync service test: `uv run pytest tests/test_sync_service.py -v` from `backend/` verifies metadata-page coordination, manual full-backfill and incremental mode selection, metadata-only raw-email persistence, retained-body preservation, public-safe batch filter decision persistence, persisted page-token resume, running and failed status reporting, expired history cursor fallback to full reconciliation, continuation page-token forwarding, ambiguity rejection when paginated sync includes a cursor without an explicit mode, retained-body selection for broad candidates and candidate thread siblings plus explicit debugging or reconciliation refs, and provider-limit chunking.
- Sync status test: `uv run pytest tests/test_sync_status.py -v` from `backend/` verifies the `SyncJobStatus` DTO, timestamp and progress validation, and the OpenAPI-documented `GET /sync/status` response.
- Email candidate query test: `uv run pytest tests/test_email_candidate_query.py -v` from `backend/` verifies broad job-search candidate scoring, public-safe signal lists, label exclusions, subject and normalized-body keyword matching, same-page thread promotion, public-safe outcome reasons, no body or snippet fields in the query, and that candidate filters stay out of provider metadata listing requests.
- Gmail provider test: `uv run pytest tests/test_gmail_email_provider.py -v` from `backend/` verifies readonly scope enforcement, advertised read-only ingestion capabilities, attachment exclusion, provider-level callback token exchange and persistence behavior, token refresh without exposed secrets, provider-level safe metadata listing, SecretStore-backed retained-body fetching, snippet exclusion, HTML-to-text normalization, empty-body failures, public-safe validation errors, and UTF-8-safe truncation.
- Gmail message listing test: `uv run pytest tests/test_gmail_message_listing.py -v` from `backend/` verifies Gmail full-backfill pagination, incremental history listing, full-backfill history cursor anchoring, expired history cursor mapping, SecretStore token access, metadata-only Gmail partial responses, metadata field normalization, public-safe provider errors with stable codes and user actions, empty pages, and the 500-message page-size limit.
  [JT-066 2026-07-05 v2] - Gmail message listing test also verifies final-page profile `historyId` cursor production.
- Secret store test: `uv run pytest tests/test_keyring_secret_store.py -v` from `backend/` verifies the default keyring-backed `SecretStore` adapter, sanitized backend failures, idempotent deletion, and factory behavior for configured backends.
- Setup status test: `uv run pytest tests/test_setup_status.py -v` from `backend/` verifies setup status reports persisted non-reauth Gmail connection metadata without exposing secrets and stays disconnected before `email_connections` exists.
- Local backend overrides: copy `backend/.env.example` to `backend/.env` only when local settings are needed; `.env` files are ignored and must not contain secrets.
- Current backend health check: `GET /health` returns `{"status": "ok"}`.
- Current setup shell: `GET /setup/status` returns typed first-run setup readiness fields plus current and recommended classification modes without reading or returning secrets, and `POST /setup` accepts non-secret first-run choices, applies the provider-based classification recommendation when `classification_mode` is omitted, validates selected provider metadata, and returns `{"status":"accepted",...}` without running provider auth flows or persisting secrets.
- Current Gmail auth endpoints: `GET /auth/gmail` returns a Google authorization URL, generated OAuth state, provider name, and the single `gmail.readonly` scope; `GET /auth/gmail/callback` exchanges the code, persists token material through `SecretStore`, stores non-secret `email_connections` metadata, and returns `EmailConnection`; provider-level token refresh keeps expired Gmail reads working while product pages remain later Phase 1 work.
- Current sync API: `POST /sync` resolves the latest non-reauth Gmail connection from `email_connections`, runs resumable full backfill until a completed `email_backfill_state` has promoted an incremental cursor, persists metadata-only `raw_emails`, upserts broad job-search filter decisions with public-safe reasons through the same batch decision path used for body retention, stores retained bodies for broad job-search candidates when the provider supports body fetching, preserves retained bodies on metadata refresh, stores cursors and resumable page progress in SQLite, returns typed `400` when no Gmail connection is configured, returns typed `409` for concurrent manual runs, and maps email-provider failures to email-specific typed responses with `user_action` details.
- Current sync status API: `GET /sync/status` returns the current or last manual run status without exposing tokens, secrets, raw cursors, page tokens, or email body content.
- Current classification estimate API: `GET /classification/estimate` returns retained candidate counts, estimated prompt/completion/total tokens, model and prompt version, token-estimate method, and cost availability without calling an LLM or returning email content.
- Current classification reprocessing API: `GET /classification/reprocessing-plan` returns retained candidate counts bucketed by current, unclassified, stale model, stale prompt version, and missing target-model setup for the configured classification target without calling an LLM or returning email content.
- Current classification run API: `POST /classification/run` executes the configured `StructuredExtractionService` batch flow over retained candidates, stores accepted classification rows and run accounting, and returns accepted and malformed counts without writing application or event rows.
- Current application detail API: `GET /applications/{id}` returns one canonical `ApplicationRecord` from the local SQLite `applications` table and returns the standard typed `404` error when the row does not exist.
- Current application events API: `GET /applications/{id}/events` returns the canonical event timeline ordered by `event_at` and `id`, returns `[]` for existing applications without events, and returns the standard typed `404` error when the parent application row does not exist.
- Current manual application edit API: `PATCH /applications/{application_id}/status` updates the status, locks the application, and writes a `status_edit` audit row; `PATCH /applications/{application_id}/events/{event_id}` updates one event, rejects no-op and missing-source-email edits, locks the application, writes an `event_edit` audit row, replays status from the edited timeline, and protects the edited event or source email from conflicting aggregation reruns.
- Current application split API: `POST /applications/{application_id}/split` accepts selected `event_ids`, optional corrected source application segmentation fields, required new application fields, and an optional reason; it moves those events into a deterministic `manual-split-*` application, manually locks both resulting applications, recalculates timeline dates, derives the new application's status from moved events, preserves an already locked source status, preserves segmentation fields used by deterministic dashboards, records a `split` correction snapshot, returns typed `404` for a missing source application, and returns typed `409` for split conflicts.
- Current application merge API: `POST /applications/{application_id}/merge` accepts a `source_application_id` and optional reason, moves source events into the target application, recalculates the merged summary, deletes the source application, records a `merge` correction snapshot, and returns typed not-found or conflict errors for invalid merge requests.
- Current deterministic metrics API: `GET /metrics/breakdown?dimension=role|source|salary|tech|sponsorship|seniority|work_mode` returns typed `MetricsBreakdownResponse` rows from local `applications` and `application_events` data without using an LLM.
- Current classification parser: `parse_llm_extraction_response` in `backend/app/pipeline/classify.py` accepts provider-neutral `LLMGenerationResponse` JSON only when the finish reason is clean, keys are unique, scalar types are strict, extracted text is non-blank, salary ranges are valid, and `event_at` is an ISO datetime string rather than a numeric timestamp.
- Current classification service: `ClassificationService` in `backend/app/services/classification.py` classifies retained `EmailClassificationCandidate` values by calling the configured `LLMProvider`, validating responses with `parse_classification_generation_response`, returning accepted and malformed result tuples, exposing storage-ready classification records through `classifications`, and aggregating prompt, completion, and total tokens.
- Malformed classification output returns `MalformedLLMExtraction` with only email ID, model, prompt version, reason, and a public-safe message; callers must quarantine or reject it without writing `email_classifications`, `applications`, or `application_events` rows.
- Current structured extraction service: `StructuredExtractionService.run_batch()` lists non-empty retained candidates stale for the configured model and prompt version, builds classification prompt requests, calls the configured `LLMProvider`, stores only accepted `email_classifications` rows, records `classification_runs` token and cost accounting, and returns accepted extraction facts plus public-safe malformed results for later pipeline stages.
- Current insight generation service: `InsightGenerationService.generate_insight()` builds deterministic fact and evidence payloads, reuses non-stale cached insights by `inputs_hash` and model, includes the insight prompt version in the input hash, and specializes the `story` prompt plus evidence scope for Q-46.
- Current aggregation service: `AggregationService.run()` groups accepted job-related extraction facts by the deterministic application grouping key, upserts one `applications` row per group, upserts deterministic `application_events`, stores event-level `extracted_status`, and derives `current_status` by replaying persisted plus current events in chronological order while preserving manual-lock conflicts.
- Current insight input and generation service: `InsightInputBuilder` prepares deterministic facts plus cited application, event, and retained-email evidence for cached Phase 4 insights; `strongest_weakest_signals` uses whole-history evidence for Q-43, and `InsightGenerationService.generate_insight()` returns matching fresh cached insights when the insight type, model, and prompt-version-aware input hash match; otherwise it calls the configured `LLMProvider`, rejects incomplete or blank output, and rejects `weekly_actions` output unless it has exactly three numbered, cited, non-citation-only action lines using known evidence citations.
- Current Gmail auth-start endpoint: `GET /auth/gmail` returns a Google authorization URL, generated OAuth state, provider name, and the single `gmail.readonly` scope; `GET /auth/gmail/callback` exchanges the code, persists token material through `SecretStore`, and stores non-secret connection metadata. Token refresh remains later Phase 1 work.
- Current retained body fetching: Gmail can fetch normalized retained bodies for selected refs behind the provider seam, manual sync stores retained bodies for broad job-search candidate messages and same-page candidate thread siblings after metadata persistence, and the raw-email repository can persist retained or debugging bodies before metadata exists. Endpoint-driven access to arbitrary retained-body refs remains later Phase 1 work.
- LLM provider setup guide: [`docs/llm-provider-setup.md`](docs/llm-provider-setup.md) documents the Azure OpenAI and Ollama values the first-run setup flow needs, including `classification_mode` choices, local-only Ollama base URL rules, and `SecretStore` boundaries.
- Current local wipe-data endpoint: `POST /local-data/wipe` removes configured local storage targets after the exact confirmation phrase `wipe-local-data`.
- Current provider registry: `app.providers.provider_registry` declares Gmail, Ollama, and Azure OpenAI metadata; validation checks selected non-secret LLM settings, rejects non-local Ollama base URLs, and does not read secret values.
- Current Ollama LLM adapter: `app.providers.llm.ollama.OllamaLLMProvider` maps provider-neutral generation DTOs to local Ollama `/api/chat` calls, supports JSON-object mode through Ollama's `format` field, maps token counts when returned, honors timeout and retry settings, and returns public-safe provider errors without logging prompt or completion content.
- Current OpenAPI schema generation: run `uv run python -m scripts.generate_openapi` from `backend/` to write sorted, indented JSON to `frontend/src/api/openapi.json`; pass `--output <path>` to write the schema elsewhere.
- Frontend OpenAPI generation script: `npm run generate:openapi` from `frontend/` shells into `backend/` and runs `uv run python -m scripts.generate_openapi`.
- Current provider config API shell: `GET /config/providers` returns the selected email provider, LLM provider, classification mode, recommended classification mode, visible non-secret provider settings, supported provider metadata, and `SecretRef` requirements without secret values.
- Current provider config update shell: `PUT /config/providers` validates and applies partial non-secret provider selection and setting changes to the running backend process only; when `llm_provider` changes and `classification_mode` is omitted, it applies the provider-based recommendation while explicit provider-valid mode choices remain supported. Durable setup persistence and secret writes are later work.
- Current Fernet fallback: `app.security.FernetSecretStore` stores encrypted secret payloads under `JOBTRACKER_DATA_DIR/secrets/` with a generated or configured `JOBTRACKER_FERNET_KEY_FILE`; `app.security.build_secret_store` returns it only when `JOBTRACKER_SECRET_STORE_BACKEND=fernet`.
- Current TypeScript API client generation: run `npm run generate:api` from `frontend/` to regenerate `src/api/openapi.json` through the backend script and then generate the Orval fetch client at `src/api/generated.ts`.
- Frontend API contract check: `npm run check` includes `check:api` so stale generated API artifacts fail before typecheck, lint, and build.
- Current backend type check: `uv run mypy` from `backend/`.
- Current frontend setup page shell: `frontend/src/pages/SetupPage.tsx` renders the `/setup` Phase 0 shell with setup checklist, provider, classification mode, Gmail read-only OAuth, privacy-boundary, setup status loading, a Gmail OAuth start action, a `Continue to Google` link, callback-complete status, and not-ready copy while real setup persistence and secret editing remain later work.
- Current frontend setup copy: `frontend/src/setupWizardCopy.ts` defines the Phase 0 card copy that the setup page shell renders for LLM provider, classification mode, Gmail read-only OAuth, and privacy-boundary choices while the full wizard flow is still scaffolded.
- Current frontend dashboard shell: `/dashboard` renders `frontend/src/pages/DashboardPage.tsx` with placeholder filter and metrics regions while backend metrics slices and dashboard data wiring continue to land incrementally.
- Current feature status dashboard: `/features` renders `frontend/src/pages/FeatureStatusDashboard.tsx` from `frontend/src/featureStatus/featureStatusRegistry.ts`, exposing implemented and in-progress frontend and backend surfaces, QA guidance, connection maps, topology summaries, and URL-backed search, status, testable, scope, and tab filters.
- Setup copy smoke test: `uv run pytest tests/test_setup_wizard_copy.py -v` from `backend/` verifies the static copy keeps the required provider, mode, Gmail, `SecretStore`, and privacy terms visible.
- Backend linting and formatting: `backend/ruff.toml` defines ruff lint and format defaults.
- Current backend lint check: run `uv run ruff check .` from `backend/`.
- Current backend format check: run `uv run ruff format --check .` from `backend/`.
- Classification parser test: `uv run pytest tests/test_classification_pipeline.py -q` from `backend/` verifies valid structured extraction plus public-safe rejection of malformed JSON, schema errors, duplicate JSON keys, unclean finish reasons, stringly typed scalars, blank extracted text, and numeric `event_at` values.
- Classification service test: `uv run pytest tests/test_classification_service.py -q` from `backend/` verifies retained candidate prompt orchestration, provider result validation, malformed-result quarantine metadata, and provider token aggregation.
- Structured extraction service test: `uv run pytest tests/test_structured_extraction_service.py -q` from `backend/` verifies the service-level LLM call, accepted-row storage, malformed-output non-storage, batch accounting, and cost behavior.
- Weekly actions insight test: `uv run --project backend pytest backend/tests/test_insight_generation_service.py -k weekly_actions -q` verifies Q-45 prompt instructions, strict three-action citation validation, citation-only rejection, and prompt-version-aware cache bypass.
- Application grouping-key test: `uv run --project backend pytest backend/tests/test_application_grouping_key.py -v` verifies normalized company and role composition, trimmed opaque thread handling, thread-first grouping, UTC time-window fallback, missing-signal handling, and positive window validation.
- Golden-set eval runner test: `uv run pytest tests/test_eval_runner.py -q` from `backend/` verifies `evaluate_golden_set` reports precision, recall, and pass/fail against the configured thresholds.
- Golden-set filter eval test: `uv run pytest tests/test_filter_eval.py -q` from `backend/` verifies `evaluate_filter_golden_set` reports precision, recall, and pass/fail against the configured thresholds.
- Pre-commit setup: run `uv run --project backend pre-commit install` from the repository root after backend and frontend dependencies are installed.
- Current pre-commit gate: run `uv run --project backend pre-commit run --all-files` from the repository root to execute backend Ruff lint, backend Ruff format check, backend mypy, and the frontend `npm run check` gate.
- Backend: `uv run` from `backend/`, with `ruff`, `mypy`, and `pytest` as the verification gate.
- Backend CI: `.github/workflows/backend-ci.yml` runs on backend and workflow changes, installs the locked `uv` environment with Python 3.12, then runs `uv run ruff check app evals tests`, `uv run mypy`, and `uv run pytest` from `backend/`.
- Frontend setup: use Node `>=22.18.0`, then run `npm install` from `frontend/`.
- Frontend dev server: `npm run dev` from `frontend/`.
- Current insights page: `/insights` renders `frontend/src/pages/Insights.tsx`, loads cached insights through the generated API client, shows Q-41 recurring recruiter feedback when cached, and can request regeneration for `recurring_feedback` while keeping counts out of the LLM path.
- Current insights backend API: `GET /insights` lists latest cached `InsightRecord` rows from local SQLite, including stale rows, and `POST /insights/regenerate` accepts an insight `type` plus optional `max_evidence_items` to force regeneration through deterministic cited inputs and the configured LLM provider.
- Frontend UI primitives: import shared buttons, text inputs, labelled form fields, alerts, tabs, and data tables from `frontend/src/components/ui`; these primitives carry the baseline accessibility behavior for later pages.
- Alert live regions: non-danger `info`, `success`, and `warning` alerts are static by default; `danger` alerts default to `role="alert"`, and callers can pass `role="status"` for dynamic non-danger status messages.
- Data table columns: scalar string, number, null, and undefined fields render directly; object-valued fields must provide a `render` callback and every table must include a caption.
- Frontend TypeScript check: `npm run typecheck` from `frontend/`.
- Frontend lint check: `npm run lint` from `frontend/`.
- Frontend unit tests: `npm run test` from `frontend/` runs Vitest with jsdom for component behavior such as UI primitive accessibility contracts and the accessible `/dashboard` shell.
- Overview sync unit tests in `frontend/src/App.test.tsx` verify current sync status loading, manual `Sync now` behavior, typed backend error display, and running-state polling.
- Setup page unit tests in `frontend/src/App.test.tsx` verify Gmail OAuth start, the read-only authorization link, and callback-complete status rendering.
- Feature status unit tests in `frontend/src/App.test.tsx` verify `/features` rendering, registry-backed frontend and backend inventory cards, topology summaries, filter persistence through `URLSearchParams`, scoped empty states, and narrow-viewport CSS constraints.
- Insights page unit tests in `frontend/src/App.test.tsx` verify `/insights` renders the cached Q-41 recurring-feedback insight returned by the generated API client.
- Current frontend route-query helper: `frontend/src/lib/routeQuery.ts` parses, serializes, and patches URL query strings for URL-backed filter state.
- Current frontend chat shell: `/chat` renders `frontend/src/pages/Chat.tsx` with disabled composer copy that reserves the page entry point without implementing streaming, history, retrieval, provider calls, or backend chat behavior.
- Frontend Playwright browser install: run `npx playwright install chromium` from `frontend/` once per machine before the browser smoke suite.
- Frontend Playwright smoke tests: run `npm run test:smoke` from `frontend/`; the suite starts Vite on `127.0.0.1:4173` and covers the current Phase 0 setup copy, overview sync affordance, and dashboard empty state.
- Frontend tooling gate: after backend dependencies are synced with `uv`, `npm run check` from `frontend/` runs API contract generation and staleness checks, typecheck, lint, Vitest, and build.
- Current frontend API boundary: import client types and helpers from `frontend/src/api`; `frontend/src/api/generated.ts` is the Orval-generated fetch client from `frontend/src/api/openapi.json`.
- Frontend CI: `.github/workflows/frontend-ci.yml` runs on pushes and pull requests to `main`, installs `uv`, sets up Python 3.12, syncs locked backend dependencies, sets up Node.js with npm caching keyed by `frontend/package-lock.json`, runs `npm ci` from `frontend/`, and runs `npm run check` from `frontend/`.
- Current frontend build check: `npm run build` from `frontend/`.
- Current frontend preview server: `npm run preview` from `frontend/` after a successful build.
- Classification changes to prompts, models, categories, extraction schemas, or parser behavior: run `uv run python -m evals.run_eval` from `backend/`; it reads `backend/evals/golden_set.jsonl`, validates records through the classification contract, reports precision and recall, and fails below 90 percent precision or 85 percent recall unless explicitly accepted.
- Heuristic filter changes: run `uv run python -m evals.run_eval --filter` from `backend/`; it reads `backend/evals/golden_set.jsonl`, validates metadata-only filter outcomes against `expected_to_pass_filter`, reports precision and recall, and fails below 90 percent precision or 85 percent recall unless explicitly accepted.
- Golden-set fixture-only changes must pass `uv run pytest tests/test_golden_set_fixture.py -v` from `backend/`.

Never claim work is complete without fresh verification evidence.

[JT-103 2026-07-05 v1] Backend role normalization: `normalize_role_title()` in `backend/app/services/normalization.py` produces deterministic Phase 2 role grouping keys without schema or API changes.
[JT-103 2026-07-05 v1] Backend role normalization test: `uv run --project backend pytest backend/tests/test_role_normalization.py -v` verifies casing, punctuation, seniority labels, title levels, abbreviations, location noise, work-arrangement notes, and missing titles.
[JT-104 2026-07-06] Backend application grouping key: `ApplicationGroupingKey` and `build_application_grouping_key()` in `backend/app/pipeline/aggregate.py` produce deterministic Phase 2 application identity signals without schema, API, or database-write changes.
[JT-104 2026-07-06] Backend application grouping-key test: `uv run --project backend pytest backend/tests/test_application_grouping_key.py -v` verifies company and role normalization composition, thread-first grouping, UTC time-window fallback, and missing-signal handling.
