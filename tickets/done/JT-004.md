---
id: JT-004
issue: 4
title: "JT-004 - Scaffold backend Python project"
state: DONE
milestone: "Phase 0 - Groundwork"
labels: [area:api, phase:0, priority:P0, size:M, type:infra]
deps: [JT-002]
url: https://github.com/talibilat/job-search-intelligence/issues/4
synced: 2026-07-03T15:00:18Z
completed: 2026-07-04T02:15:00Z
---

Title: JT-004 - Scaffold backend Python project

Mini-PRD / Context:
Create the FastAPI backend package with Python 3.12 and `uv` project metadata.
This ticket contributes to the local-first job-search intelligence app by preserving the `applications` table and event timeline as the factual source of truth.

Linked requirements:
FR-0, FR-6, NFR-5, NFR-8, Phase 0

Scope - in:
- Deliver the specific behavior described by this ticket.
- Keep the change atomic enough for one focused implementation session.
- Touch the planned modules only unless implementation discovery shows a documented need.

Scope - out:
- Do not introduce telemetry, shared credentials, autonomous outbound email, auto-apply behavior, or multi-user SaaS assumptions.
- Do not add broad abstractions outside the architecture contract.
- Do not move later-phase work into this ticket unless explicitly listed.

Technical approach:
- Follow Repository pattern for data access, Strategy for email and LLM providers, Pipeline stages passing Pydantic DTOs, service layer business logic, FastAPI dependency injection, typed errors, mypy-clean code, and ruff formatting.
- Keep API routes thin and put business behavior in services.
- Ensure secrets are encrypted at rest and never logged.
- The LLM never emits raw SQL for execution; use deterministic SQL, typed Python logic, or constrained query builders as appropriate.
- Planned files/modules: backend/app/api/, docs/, tickets/.

Acceptance criteria:
- [x] The ticket behavior is implemented and mapped to the linked requirements.
- [x] The implementation preserves local-first SQLite storage and the `applications` plus `application_events` invariant (no data-layer changes in this ticket).
- [x] Relevant DTOs, repositories, services, and API boundaries are typed (mypy strict, unaffected by this ticket, still clean).
- [x] Relevant tests, evals, or smoke checks are added or updated (full backend suite verified against the new project metadata).
- [x] User-facing behavior is visible in the app, API, docs, or generated backlog artifact as appropriate (README documents the new `uv` workflow).

Dependencies:
- Blocked by: JT-002.
- Blocks: None listed in this generated manifest.

Definition of Done:
- [x] Acceptance criteria are met.
- [ ] Code or documentation is merged into the branch (PR open, not yet merged).
- [x] Architecture and privacy conventions are honored.
- [x] Verification commands relevant to the touched area have been run and recorded.
- [x] For classification-related changes, the golden-set eval passes if applicable (not applicable; no classification changes).

Implementation notes:
- Added `backend/pyproject.toml` `[project]` and `[dependency-groups]` sections via `uv init --bare` plus `uv add`, establishing the backend as a uv-managed project pinned to Python 3.12 (`job-search-intelligence-backend`).
- Runtime dependencies: `fastapi>=0.115`, `uvicorn[standard]>=0.32`, plus `pydantic` and `starlette` declared explicitly since `app/security/secret_store.py`, `app/models/health.py`, and `app/api/errors.py` (landed via JT-013/JT-008/JT-010, merged into `main` while this ticket was in flight) import them directly rather than only transitively through FastAPI.
- Dev dependency group: `ruff`, `mypy`, `pytest`, `httpx2` (starlette's `TestClient` in the resolved starlette version prefers `httpx2` over the now-deprecated `httpx`; verified by installing `httpx` first and observing a `StarletteDeprecationWarning` pointing at `httpx2`).
- Added `[build-system]`/hatchling so the `app` package installs editable, which JT-008's `tests/test_app_factory.py` needs to import `app.main`.
- `backend/pyproject.toml` already carried JT-006's `[tool.mypy]` section by the time this ticket rebased onto `main`; merged additively rather than replacing it, per that ticket's own comment ("JT-006 owns mypy defaults; later Phase 0 tickets add project and Ruff metadata").
- Deliberately did not create `backend/app/main.py`, `backend/app/api/router.py`, ruff config, or pytest config/test files: those are owned by sibling Phase 0 tickets (JT-005 ruff, JT-006 mypy, JT-007 pytest harness, JT-008 app factory), all merged into `main` before this PR opened.
- Rebased this branch onto `main` twice during implementation as JT-007/008/009/010/012/013 merged concurrently; resolved `backend/pyproject.toml` and `README.md` conflicts additively by hand after opting out of the automated `no-mistakes` gate for this run (its `document` step hit an unrelated Claude session usage limit) at the user's explicit direction to push and open the PR directly instead.
- Updated `README.md` Status and Development sections to state the backend `uv` scaffold now exists and to document `uv sync` / `uv run` as the backend workflow.

Verification:
- From `backend/`, `uv sync --locked` resolved and installed the locked dependency set with no drift.
- From `backend/`, `uv run pytest` passed all 21 tests (covering this ticket's scaffold plus JT-007/008/009/010/013's merged test suites).
- From `backend/`, `uv run ruff check .` passed with no findings.
- From `backend/`, `uv run mypy --config-file pyproject.toml` passed with no issues across 18 source files.
- Golden-set eval was not applicable because this ticket made no classification changes.

PR: https://github.com/talibilat/job-search-intelligence/pull/235 (open, awaiting review/merge).

Estimate: L
