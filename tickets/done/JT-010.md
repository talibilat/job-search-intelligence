---
id: JT-010
issue: 10
title: "JT-010 - Add typed API error model"
state: DONE
milestone: "Phase 0 - Groundwork"
labels: [area:api, phase:0, priority:P0, size:M, type:infra]
deps: [JT-002]
url: https://github.com/talibilat/job-search-intelligence/issues/10
synced: 2026-07-03T15:00:18Z
completed: 2026-07-04T01:13:10Z
---

Title: JT-010 - Add typed API error model

Mini-PRD / Context:
Establish typed API error DTOs and exception mapping.
This ticket contributes to the local-first job-search intelligence app by preserving the `applications` table and event timeline as the factual source of truth.

Linked requirements:
FR-0, FR-6, NFR-5, NFR-8, Phase 0

Scope - in:
- Deliver the specific behavior described by this ticket.
- Keep the change atomic enough for one focused implementation session.
- Touch the planned modules only unless implementation discovery shows a documented need.

Scope - out:
- Do not introduce telemetry, shared credentials, autonomous outbound email, auto-apply behavior, or multi-user SaaS assumptions.
- Do not add broad abstractions outside the architecture contract.
- Do not move later-phase work into this ticket unless explicitly listed.

Technical approach:
- Follow Repository pattern for data access, Strategy for email and LLM providers, Pipeline stages passing Pydantic DTOs, service layer business logic, FastAPI dependency injection, typed errors, mypy-clean code, and ruff formatting.
- Keep API routes thin and put business behavior in services.
- Ensure secrets are encrypted at rest and never logged.
- The LLM never emits raw SQL for execution; use deterministic SQL, typed Python logic, or constrained query builders as appropriate.
- Planned files/modules: backend/app/api/, docs/, tickets/.

Acceptance criteria:
- [x] The ticket behavior is implemented and mapped to the linked requirements.
- [x] The implementation preserves local-first SQLite storage and the `applications` plus `application_events` invariant.
- [x] Relevant DTOs, repositories, services, and API boundaries are typed.
- [x] Relevant tests, evals, or smoke checks are added or updated.
- [x] User-facing behavior is visible in the app, API, docs, or generated backlog artifact as appropriate.

Dependencies:
- Blocked by: JT-002.
- Blocks: None listed in this generated manifest.

Definition of Done:
- [x] Acceptance criteria are met.
- [x] Code or documentation is merged into the branch.
- [x] Architecture and privacy conventions are honored.
- [x] Verification commands relevant to the touched area have been run and recorded.
- [x] For classification-related changes, the golden-set eval passes if applicable.

Estimate: M

Implementation notes:
- Added typed Pydantic API error DTOs for the standard `{"error": ...}` response shape.
- Added a typed `ApiError` exception for future route and service code to raise explicit API-boundary errors.
- Registered FastAPI exception handlers for `ApiError`, request validation errors, Starlette HTTP errors, and unhandled exceptions.
- Kept validation and internal-error responses sanitized by omitting raw request input, traceback data, and raw exception text.
- Kept unmapped HTTP exception details sanitized by requiring route-specific public messages to use `ApiError` instead of arbitrary `HTTPException.detail`.
- Kept the implementation scoped to the API boundary and app factory registration.

Verification:
- From `backend/`, baseline `python3 -m pytest -v` passed with 3 tests before JT-010 edits.
- From `backend/`, baseline `mypy --config-file pyproject.toml` passed before JT-010 edits.
- From `backend/`, baseline `ruff check .` passed before JT-010 edits.
- From `backend/`, `python3 -m pytest tests/test_api_errors.py -v` first failed because `app.api.errors` did not exist.
- From `backend/`, after adding the smallest importable stub, `python3 -m pytest tests/test_api_errors.py -v` failed on the missing typed error behavior.
- From `backend/`, code-quality review identified a leak path for unmapped non-5xx `HTTPException.detail`; `python3 -m pytest tests/test_api_errors.py::test_unmapped_http_exception_detail_is_sanitized -v` failed before the fix.
- From `backend/`, final `python3 -m pytest tests/test_api_errors.py -v` passed with 5 tests.
- From `backend/`, final `python3 -m pytest -v` passed with 8 tests.
- From `backend/`, final `mypy --config-file pyproject.toml` passed.
- From `backend/`, final `ruff check .` passed.
- Golden-set eval was not applicable because JT-010 made no classification changes.
