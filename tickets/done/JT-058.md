---
id: JT-058
issue: 58
title: "JT-058 - Add Gmail provider skeleton"
state: DONE
milestone: "Phase 1 - Gmail Ingestion"
labels: [area:ingestion, phase:1, priority:P0, size:M, type:feature]
deps: [JT-013, JT-017, JT-026]
url: https://github.com/talibilat/job-search-intelligence/issues/58
completed: 2026-07-05T12:00:00Z
---

Title: JT-058 - Add Gmail provider skeleton

Mini-PRD / Context:
Implement the `EmailProvider` adapter boundary for Gmail.
This ticket contributes to the local-first job-search intelligence app by preserving the `applications` table and event timeline as the factual source of truth.

Linked requirements:
FR-1.1, FR-1.4, FR-1.6, FR-6, NFR-1, NFR-4, NFR-7, Phase 1.

Scope - in:
- Add the concrete `GmailEmailProvider` skeleton behind the existing `EmailProvider` boundary.
- Export the Gmail skeleton from `app.providers.email`.
- Advertise intended read-only Gmail ingestion capabilities without calling live Google APIs.
- Enforce the v1 `gmail.readonly` scope and attachment exclusion policy.
- Return public-safe typed provider errors for runtime operations deferred to later tickets.

Scope - out:
- Do not introduce telemetry, shared credentials, autonomous outbound email, auto-apply behavior, or multi-user SaaS assumptions.
- Do not add broad abstractions outside the architecture contract.
- Do not move later-phase work into this ticket unless explicitly listed.
- Do not implement live Gmail OAuth, token refresh, metadata listing, retained body fetching, sync orchestration, database writes, or API routes.

Technical approach:
- Follow the existing `EmailProvider` Strategy boundary and Pydantic DTOs.
- Keep runtime Gmail behavior behind public-safe `EmailProviderError` placeholders until the later Gmail runtime tickets.
- Ensure OAuth callback codes, token references, mailbox addresses, and email body content are not included in skeleton error text.
- Planned files/modules: `backend/app/providers/email/` and focused backend tests.

Acceptance criteria:
- [x] The ticket behavior is implemented and mapped to the linked requirements.
- [x] The implementation preserves local-first SQLite storage and the `applications` plus `application_events` invariant by avoiding database writes.
- [x] Relevant provider boundaries are typed through the existing `EmailProvider` protocol and DTOs.
- [x] Relevant tests are added for protocol conformance, readonly scopes, capabilities, attachment exclusion, and public-safe runtime errors.
- [x] User-facing behavior is visible in the exported backend provider module and docs.

Dependencies:
- Blocked by: JT-013, JT-017, JT-026.
- Blocks: None listed in this generated manifest.

Definition of Done:
- [x] Acceptance criteria are met.
- [x] Code or documentation is merged into the branch.
- [x] Architecture and privacy conventions are honored.
- [x] Verification commands relevant to the touched area have been run and recorded.
- [x] For classification-related changes, the golden-set eval passes if applicable.

Implementation notes:
- Added `backend/app/providers/email/gmail.py` with `GmailEmailProvider`.
- Exported `GmailEmailProvider` from `app.providers.email`.
- Enforced the configured Gmail scopes to exactly `https://www.googleapis.com/auth/gmail.readonly`.
- Advertised Gmail capabilities for OAuth, full backfill, incremental sync, ignored attachments, configured metadata page size, and a bounded retained-body batch size.
- Kept live Gmail OAuth, refresh, metadata listing, and retained body fetching deferred behind public-safe `EmailProviderError` placeholders.
- Added `backend/tests/test_gmail_email_provider.py` for protocol conformance, scope validation, capability advertisement, attachment exclusion, and secret/body-safe error behavior.

Verification:
- `uv run pytest tests/test_gmail_email_provider.py -v` passed from `backend/`.
- Golden-set eval was not applicable because JT-058 made no classification changes.

Estimate: L
