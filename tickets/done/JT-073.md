---
id: JT-073
issue: 73
title: "JT-073 - Add sync service"
state: DONE
milestone: "Phase 1 - Gmail Ingestion"
labels: [area:ingestion, phase:1, priority:P0, size:M, type:feature]
deps: [JT-013, JT-017, JT-026, JT-069]
url: https://github.com/talibilat/job-search-intelligence/issues/73
completed: 2026-07-05T00:00:00Z
---

Title: JT-073 - Add sync service

Mini-PRD / Context:
Orchestrate manual sync, metadata backfill, incremental cursor selection, expired-cursor recovery, and status updates.
[JT-069 2026-07-05 v2] `backend/app/services/sync_service.py` already contains the sync-cursor service seam; this ticket should extend that service for orchestration instead of replacing the cursor persistence flow.
This ticket contributes to the local-first job-search intelligence app by preserving the `applications` table and event timeline as the factual source of truth.

Linked requirements:
FR-1, FR-6, NFR-1, NFR-4, NFR-7, Phase 1

Scope - in:
- Deliver manual metadata sync orchestration for configured email connections.
- Persist metadata-only `raw_emails` rows without overwriting retained bodies.
- Persist provider cursors and in-progress page tokens through the sync-state repository.
- Expose current and last-run status for manual sync.
- Keep the change atomic enough for one focused implementation session.
- Touch the planned modules only unless implementation discovery shows a documented need.

Scope - out:
- Do not introduce telemetry, shared credentials, autonomous outbound email, auto-apply behavior, or multi-user SaaS assumptions.
- Do not add broad abstractions outside the architecture contract.
- Do not move later-phase work into this ticket unless explicitly listed.

Technical approach:
- Follow Repository pattern for data access, Strategy for email and LLM providers, Pipeline stages passing Pydantic DTOs, service layer business logic, FastAPI dependency injection, typed errors, mypy-clean code, and ruff formatting.
- Keep API routes thin and put business behavior in services.
- Ensure secrets are encrypted at rest and never logged.
- The LLM never emits raw SQL for execution; use deterministic SQL, typed Python logic, or constrained query builders as appropriate.
- Reuse `SyncStateRepository` and the existing `SyncService` cursor methods when adding manual, backfill, incremental, and status orchestration.
- Planned files/modules: backend/app/api/, backend/app/providers/email/, backend/app/services/sync_service.py, docs/, tickets/.

Acceptance criteria:
- [x] The ticket behavior is implemented and mapped to the linked requirements.
- [x] The implementation preserves local-first SQLite storage and the `applications` plus `application_events` invariant.
- [x] Relevant DTOs, repositories, services, and API boundaries are typed.
- [x] Relevant tests, evals, or smoke checks are added or updated.
- [x] User-facing behavior is visible in the API and docs.

Dependencies:
- Blocked by: JT-013, JT-017, JT-026, JT-069.
- Blocks: None listed in this generated manifest.

Definition of Done:
- [x] Acceptance criteria are met.
- [x] Code or documentation is merged into the branch.
- [x] Architecture and privacy conventions are honored.
- [x] Verification commands relevant to the touched area have been run and recorded.
- [x] For classification-related changes, the golden-set eval passes if applicable.

Implementation notes:
- Added manual sync orchestration through `EmailSyncService` and `POST /sync` dependency wiring.
- Added `GET /sync/status` for current or last-run status with provider, account, mode, counts, timestamps, expired-cursor recovery, and public error text.
- Added metadata-only `raw_emails` persistence that preserves already-retained bodies on refresh.
- Added `raw_emails` migration support and extended sync-state persistence for in-progress page tokens.
- Left Gmail OAuth callback, token persistence, connected-account lookup, concrete Gmail incremental transport, retained-body fetching, classification, and aggregation to neighboring Phase 1 or later tickets.

Verification:
- Backend sync API and sync service tests cover manual full backfill, incremental selection, page-progress resume, typed not-configured and concurrency errors, metadata persistence, retained-body preservation, failed status, and expired-cursor fallback.
- Golden-set eval was not applicable because JT-073 made no classification changes.

Estimate: L
