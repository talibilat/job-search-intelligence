---
id: JT-074
issue: 74
title: "JT-074 - Connect sync API to persisted Gmail account"
state: DONE
milestone: "Phase 1 - Gmail Ingestion"
labels: [area:api, area:ingestion, phase:1, priority:P0, size:M, type:feature]
deps: [JT-013, JT-017, JT-026, JT-073]
url: https://github.com/talibilat/job-search-intelligence/issues/74
completed: 2026-07-05T00:00:00Z
---

Title: JT-074 - Connect sync API to persisted Gmail account

Mini-PRD / Context:
Connect the existing `POST /sync` and `GET /sync/status` routes to persisted Gmail connection state once OAuth callback and token persistence land.
JT-073 added the route shell, manual sync runtime seam, typed not-configured response, concurrency response, and status DTO.
JT-067 replaces the temporary unconfigured dependency with real connected-account lookup and full-backfill runtime selection.
This ticket contributes to the local-first job-search intelligence app by preserving the `applications` table and event timeline as the factual source of truth.

Linked requirements:
FR-1, FR-6, NFR-1, NFR-4, NFR-7, Phase 1

Scope - in:
- Resolve the configured Gmail account for the current local user without exposing token material.
- Inject an `EmailConnection` into the existing sync runtime when Gmail connection persistence is available.
- Preserve the existing typed `400` response when no Gmail account is connected.
- Keep the change atomic enough for one focused implementation session.
- Touch the planned modules only unless implementation discovery shows a documented need.

Scope - out:

- Do not introduce telemetry, shared credentials, autonomous outbound email, auto-apply behavior, or multi-user SaaS assumptions.
- Do not add broad abstractions outside the architecture contract.
- Do not move later-phase work into this ticket unless explicitly listed.

Technical approach:

- Follow Repository pattern for data access, Strategy for email and LLM providers, Pipeline stages passing Pydantic DTOs, service layer business logic, FastAPI dependency injection, typed errors, mypy-clean code, and ruff formatting.
- Keep API routes thin and put business behavior in services.
- Ensure secrets are encrypted at rest and never logged.
- The LLM never emits raw SQL for execution; use deterministic SQL, typed Python logic, or constrained query builders as appropriate.
- Reuse the existing `POST /sync` and `GET /sync/status` route handlers from JT-073 instead of adding duplicate routes.
- Planned files/modules: backend/app/api/, backend/app/providers/email/, backend/app/services/sync_service.py, docs/, tickets/.

Acceptance criteria:
- [x] Persisted Gmail connection lookup is wired into the existing sync API routes.
- [x] Missing Gmail connection state still returns the existing typed not-configured `400`.
- [x] The implementation preserves local-first SQLite storage and the `applications` plus `application_events` invariant.
- [x] Relevant DTOs, repositories, services, and API boundaries are typed.
- [x] Relevant tests, evals, or smoke checks are added or updated.
- [x] User-facing behavior is visible in the app, API, docs, or generated backlog artifact as appropriate.

Dependencies:

- Blocked by: JT-013, JT-017, JT-026, JT-073.
- Blocks: None listed in this generated manifest.

Definition of Done:

- [x] Acceptance criteria are met.
- [x] Code or documentation is merged into the branch.
- [x] Architecture and privacy conventions are honored.
- [x] Verification commands relevant to the touched area have been run and recorded.
- [x] For classification-related changes, the golden-set eval passes if applicable.

Implementation notes:

- Added default Gmail connection lookup through `EmailConnectionRepository.fetch_default_connection_metadata`.
- Kept the typed not-configured `400` when the database or `email_connections` table has no usable Gmail connection.
- Wired the configured sync runtime to run full backfill until a completed backfill promotes an incremental cursor, then use incremental sync.

Verification:

- `backend/tests/test_sync_api.py` covers persisted Gmail connection lookup, not-configured behavior, full-backfill runtime selection, and concurrency handling.
- Golden-set eval was not applicable because JT-074 made no classification changes.

Estimate: M
