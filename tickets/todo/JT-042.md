---
id: JT-042
issue: 42
title: "JT-042 - Add shared accessible UI primitives"
state: OPEN
milestone: "Phase 0 - Groundwork"
labels: [area:infra, phase:0, priority:P0, size:M, type:infra]
deps: [JT-002]
url: https://github.com/talibilat/job-search-intelligence/issues/42
synced: 2026-07-03T15:00:18Z
---

Title: JT-042 - Add shared accessible UI primitives

Mini-PRD / Context:
Create buttons, inputs, forms, alerts, tabs, and table primitives.
This ticket contributes to the local-first job-search intelligence app by preserving the `applications` table and event timeline as the factual source of truth.

Linked requirements:
FR-0, FR-6, NFR-5, NFR-8, Phase 0

Scope - in:
- Deliver the specific behavior described by this ticket.
- Keep the change atomic enough for one focused implementation session.
- Touch the planned modules only unless implementation discovery shows a documented need.

Scope - out:
- Do not introduce telemetry, shared credentials, autonomous outbound email, auto-apply behavior, or multi-user SaaS assumptions.
- Do not add broad abstractions outside the architecture contract.
- Do not move later-phase work into this ticket unless explicitly listed.

Technical approach:
- Add shared React primitives for buttons, text inputs, labelled form fields, alerts, tabs, and data tables under `frontend/src/components/ui/`.
- Keep accessibility behavior inside the primitives so later setup, dashboard, insights, and chat pages do not duplicate label wiring, live-region roles, keyboard tab navigation, or table captions.
- Keep styling aligned with the existing frontend shell and avoid backend, database, pipeline, LLM, or application timeline changes.
- Add focused frontend tests or usage contracts for accessibility-sensitive behavior.
- Planned files/modules: `frontend/src/components/ui/`, `frontend/package.json`, `frontend/vite.config.ts`, `README.md`, `docs/`.

Acceptance criteria:
- [ ] Shared `Button`, `TextInput`, `FormField`, `Alert`, `Tabs`, and `DataTable` primitives are exported from `frontend/src/components/ui/`.
- [ ] Labels, descriptions, errors, alert roles, tab keyboard navigation, and table captions are handled inside the primitives.
- [ ] The implementation preserves local-first SQLite storage and the `applications` plus `application_events` invariant by staying frontend-only.
- [ ] Relevant frontend tests, type checks, or usage contracts are added or updated.
- [ ] Documentation describes the primitives and the frontend test command.

Dependencies:
- Blocked by: JT-002.
- Blocks: None listed in this generated manifest.

Definition of Done:
- [ ] Acceptance criteria are met.
- [ ] Code or documentation is merged into the branch.
- [ ] Architecture and privacy conventions are honored.
- [ ] Verification commands relevant to the touched area have been run and recorded.
- [ ] For classification-related changes, the golden-set eval passes if applicable.

Estimate: M
