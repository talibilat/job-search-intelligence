---
id: JT-053
issue: 53
title: "JT-053 - Add setup wizard copy"
state: OPEN
milestone: "Phase 0 - Groundwork"
labels: [area:infra, area:ingestion, area:setup, phase:0, priority:P0, size:M, type:docs]
deps: [JT-002]
url: https://github.com/talibilat/job-search-intelligence/issues/53
synced: 2026-07-03T15:00:18Z
---

Title: JT-053 - Add setup wizard copy

Mini-PRD / Context:
Add precise setup-screen content for provider, mode, Gmail, and privacy choices.
This ticket contributes to the local-first job-search intelligence app by preserving the `applications` table and event timeline as the factual source of truth.

Linked requirements:
FR-0, FR-6, NFR-5, NFR-8, Phase 0

Scope - in:
- Deliver the specific behavior described by this ticket.
- Keep the change atomic enough for one focused implementation session.
- Touch the planned modules only unless implementation discovery shows a documented need.

Scope - out:
- Do not introduce telemetry, shared credentials, autonomous outbound email, auto-apply behavior, or multi-user SaaS assumptions.
- Do not add broad abstractions outside the architecture contract.
- Do not move later-phase work into this ticket unless explicitly listed.

Technical approach:
- Define typed frontend setup copy for the Phase 0 shell in `frontend/src/setupWizardCopy.ts`.
- Render provider, mode, Gmail, and privacy-boundary cards from the copy module in `frontend/src/App.tsx`.
- Keep the copy explicit about Azure OpenAI, Ollama, `hybrid`, `llm`, `local`, Gmail `gmail.readonly`, user-owned OAuth clients, `SecretStore`, no shared credentials, no telemetry, and deterministic dashboard counts.
- Add a documentation-style backend smoke test that verifies the required setup-copy terms remain present.
- Do not implement OAuth, provider adapters, setup submission, credential storage, or backend behavior in this ticket.

Acceptance criteria:
- [x] The ticket behavior is implemented and mapped to the linked requirements.
- [x] The implementation preserves local-first SQLite storage and the `applications` plus `application_events` invariant.
- [x] The setup copy is typed at the frontend boundary.
- [x] Relevant tests, evals, or smoke checks are added or updated.
- [x] User-facing behavior is visible in the app, API, docs, or generated backlog artifact as appropriate.

Implementation notes:
- Added `frontend/src/setupWizardCopy.ts` with typed setup-copy sections and options.
- Added static Phase 0 setup-copy cards to `frontend/src/App.tsx` and responsive styles to `frontend/src/index.css`.
- Added `backend/tests/test_setup_wizard_copy.py` to guard required setup-copy coverage.

Dependencies:
- Blocked by: JT-002.
- Blocks: None listed in this generated manifest.

Definition of Done:
- [ ] Acceptance criteria are met.
- [ ] Code or documentation is merged into the branch.
- [ ] Architecture and privacy conventions are honored.
- [ ] Verification commands relevant to the touched area have been run and recorded.
- [ ] For classification-related changes, the golden-set eval passes if applicable.

Estimate: S
